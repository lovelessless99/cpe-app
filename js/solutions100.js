/* 第六十批 —— pdftotext 重抽題敘後補回 */
const SOL100 = {
  '10060': {
    q: `MCMC 用鋼板做人孔蓋。他們進口各種形狀與厚度的多邊形鋼板，熔掉之後做成圓形的人孔蓋（人孔蓋也有各自的大小與厚度）。給你若干塊鋼板的尺寸，請算出可以做出幾個人孔蓋。

輸入：檔案由若干資料區塊組成。每個區塊先是一個整數 N（多邊形鋼板的數量），接下來 N 行、每行先是該鋼板的厚度 Ti，接著是多邊形各頂點的座標（順時針或逆時針皆可），格式是
    Ti X0 Y0 X1 Y1 X2 Y2 … Xn Yn X0 Y0
注意最後會**重複第一個點的座標**。每個區塊的最後一行是兩個實數 R 與 T，分別是人孔蓋的半徑與厚度。
以 N = 0 的區塊結束。
輸出：每個區塊一行，輸出可以做出的人孔蓋數量。

範例輸入
2
2 0 0 0 10 5 15 12 10 10 0 0 0
5 0 0 5 100 100 0 0 0
5 3
1
2 0 0 10 0 10 10 0 10 0 0
5 2
0

範例輸出
107
1`,
    h: `題目講了一堆背景，本質上只是「總體積除以單個人孔蓋的體積再取下高斯」。

    每塊鋼板的體積 = 多邊形面積 × 厚度
    多邊形面積用鞋帶公式：|Σ (xᵢ·yᵢ₊₁ − xᵢ₊₁·yᵢ)| / 2
    人孔蓋體積 = π·R²·T
    答案 = ⌊ 總體積 / 人孔蓋體積 ⌋

【逐組驗算】（兩組範例我都手算過）
  第 1 組：
      鋼板一：頂點 (0,0)、(0,10)、(5,15)、(12,10)、(10,0)，鞋帶算出面積 140，
              乘上厚度 2 → 體積 280
      鋼板二：頂點 (0,0)、(5,100)、(100,0)，面積 5000，乘上厚度 5 → 體積 25000
      總體積 = 25280
      人孔蓋 R = 5、T = 3 → 體積 = π × 25 × 3 = 235.619
      25280 / 235.619 = 107.29 → **107** ✓
  第 2 組：
      鋼板：邊長 10 的正方形，面積 100，乘上厚度 2 → 體積 200
      人孔蓋 R = 5、T = 2 → 體積 = π × 25 × 2 = 157.08
      200 / 157.08 = 1.27 → **1** ✓

【讀取要小心】每一行的數字個數不固定，而且最後一個點會重複第一個點。
所以要**整行讀進來再切 token**：第一個 token 是厚度，剩下的兩兩一組是座標，
最後那一對重複的直接忽略（或留著也不影響鞋帶公式，因為重複點的貢獻是 0）。`,
    t: `1. 每行的座標數量不固定，一定要整行讀進來再切 token，不能用固定次數的 cin >>。
2. 最後一個點重複第一個點；留著也沒關係（鞋帶公式對重複的相鄰點貢獻為 0），但如果你用「頂點數 = token 數 / 2」去算就要記得扣掉。
3. 鞋帶公式要取絕對值，因為題目說頂點順時針或逆時針都有可能。
4. 人孔蓋是圓柱體，體積是 π·R²·T，不是 π·R²（忘了乘厚度會差很多）。
5. 答案要取下高斯（做不出半個蓋子）。
6. 座標與厚度都是實數，全程用 double。
7. 結束條件是 N = 0，那個區塊不需要輸出。`,
    c: `#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    const double PI = acos(-1.0);
    int n;
    string line;
    while (cin >> n && n != 0) {
        getline(cin, line);                        // 吃掉 N 那行剩下的部分
        double total = 0;
        for (int i = 0; i < n; i++) {
            getline(cin, line);
            istringstream in(line);
            vector<double> v;
            double t;
            while (in >> t) v.push_back(t);
            double thick = v[0];
            int m = (int)(v.size() - 1) / 2;        // 座標對數（含重複的首點）
            double area = 0;
            for (int k = 0; k < m; k++) {
                double x1 = v[1 + 2 * k], y1 = v[2 + 2 * k];
                int k2 = (k + 1) % m;
                double x2 = v[1 + 2 * k2], y2 = v[2 + 2 * k2];
                area += x1 * y2 - x2 * y1;          // 鞋帶公式
            }
            area = fabs(area) / 2.0;
            total += area * thick;
        }
        double R, T;
        cin >> R >> T;
        getline(cin, line);
        double one = PI * R * R * T;                // 圓柱體積
        cout << (long long)floor(total / one) << "\n";
    }
    return 0;
}`
  },

  '10064': {
    q: `四維空間裡有一條安全的直線，Arnold 的朋友們都把房子蓋在這條線上（或旁邊）。線上用一維座標表示，兩點 d1、d2 的距離是 |d1 − d2|，移動的花費與距離的**平方**成正比。

Arnold 要把家蓋在「拜訪朋友所花的力氣最小」的地方。他從家出發，走到一位朋友家、在那裡停下來，再回家、停下來，再去下一位朋友家……最後回到家。

輸入：若干區塊，每個區塊先是整數 N（0 ≤ N ≤ 10000）代表朋友數，接著是 N 個朋友家的座標。讀到檔案結束為止。
輸出：每個區塊一行，輸出兩個座標：第一個是「每一站都停下來」時的最佳位置，第二個是「整趟旅程連續不停」時的最佳位置，中間以一個空白分隔。若無法決定就印「-1」。（若其中一個能決定，另一個也一定能決定。）若最佳位置不只一個，印最小的那個。

範例輸入
1 100
1 200
2 300 300

範例輸出
100.00 100.00
200.00 200.00
300.00 300.00`,
    h: `題目最後那段附註是關鍵提示：停下來比較省力，因為
    (AB² + BC²) < AC²   （當 B 在 A 與 C 之間時）
也就是說「花費 ∝ 距離平方」時，把一段路拆開走反而更省。

【第一個座標：每一站都停下來】
每趟「家 → 朋友 i → 家」都是兩段各自獨立的移動，所以總花費是

    Σ 2·(x − dᵢ)²

要最小化這個式子，對 x 微分得 x = **平均數**（算術平均）。

【第二個座標：整趟連續不停】
如果全程不停，整趟旅程就是**一段連續的移動**，總長度是

    L = Σ 2·|x − dᵢ|

花費 ∝ L²。因為 L ≥ 0，最小化 L² 等於最小化 L，而最小化「到各點距離之和」的位置是
**中位數**。當 N 是偶數時，兩個中位數之間的任何點都同樣好，題目要「印最小的」，
所以取**較小的那一個中位數**（排序後的第 (N−1)/2 個，0-based）。

【無法決定的情形】N = 0（一個朋友都沒有）時，任何位置都一樣好，無法決定 → 印 −1。

【驗算】三組範例的朋友座標都相同（100 / 200 / 300 與 300 300），
所以平均數與中位數自然相等，輸出 100.00 100.00、200.00 200.00、300.00 300.00 ✓ 全中。

【輸出】兩個數都取小數點後兩位。`,
    t: `1. 兩個答案的意義完全不同：「每站都停」是最小化平方和 → 平均數；「連續不停」是最小化總長度 → 中位數。範例三組的資料剛好讓兩者相等，很容易誤以為答案永遠一樣。
2. N 是偶數時中位數有一整段區間都最佳，題目要求印最小的，所以取排序後第 (N−1)/2 個（0-based），不要取兩個中位數的平均。
3. N = 0 時輸出 −1（題目說若一個能決定另一個也能，所以只會整行都是 −1）。
4. 「家 → 朋友 → 家」是兩段，所以係數是 2；不過係數不影響最小值的位置，可以直接忽略。
5. 座標可能是實數也可能很大，用 double 讀；N 可到 10000，記得用快速輸入。
6. 兩個輸出都要兩位小數，中間一個空白。
7. 讀到 EOF 結束，不要假設區塊數量。`,
    unsure: true,
    c: `#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout << fixed << setprecision(2);
    int n;
    while (cin >> n) {
        vector<double> d(n);
        double sum = 0;
        for (int i = 0; i < n; i++) { cin >> d[i]; sum += d[i]; }
        if (n == 0) { cout << "-1\n"; continue; }
        double mean = sum / n;                       // 每站都停 -> 最小化平方和
        sort(d.begin(), d.end());
        double med = d[(n - 1) / 2];                 // 連續不停 -> 最小化總長度，取較小的中位數
        cout << mean << " " << med << "\n";
    }
    return 0;
}`
  }
};
