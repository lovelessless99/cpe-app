/* 第二十四批 —— pdftotext 重抽題敘後補回 */
const SOL64 = {
  '10232': {
    q: `把所有「小於 2^31 的非負整數」依下列規則排成一個數列 B：

  1. 二進位中 1 的個數（記為 O(x)）比較少的排在前面；
  2. 1 的個數相同時，數值小的排在前面。

也就是說，對任意 n < m 都有：O(B_n) < O(B_m)，或者 O(B_n) = O(B_m) 且 B_n < B_m。

給定 n（0 ≤ n ≤ 2147483647），請求出 B_n。

輸入：每行一個非負整數 n，讀到 EOF。
輸出：每行輸出對應的 B_n。

範例輸入
0
1
2
31
32

範例輸出
0
1
2
1073741824
3`,
    h: `因為只考慮「小於 2^31」的數，也就是最多 31 個位元，所以「恰好有 k 個 1」的數共有 C(31, k) 個。

【第一步：決定 k】
把 C(31,0), C(31,1), ... 累加，找出 n 落在哪一組：
    cum(k−1) ≤ n < cum(k)
那 B_n 就是「恰好 k 個 1」那一組中，由小到大排第 r = n − cum(k−1) 個（0-based）。

【第二步：在該組中「解排名」】
從最高位（第 30 位）往下走，維護「還需要幾個 1」= need：
    在第 b 位時，若這一位放 0，剩下的低 b 位要放 need 個 1，共有 C(b, need) 種；
    若 r < C(b, need) → 這一位是 0；
    否則 r −= C(b, need)，這一位是 1，need−−。
走完 31 位就得到答案。

【驗算】
    C(31,0) = 1 → 索引 0 是 0（一個 1 都沒有）✓
    C(31,1) = 31 → 索引 1..31 是 1, 2, 4, ..., 2^30
        索引 31 → 組內第 30 個（0-based）→ 2^30 = 1073741824 ✓（與範例一致）
    索引 32 進入 k=2 的第一個 → 最小的兩個 1 的數 = 3 ✓
五組範例全中。

【數值範圍】C(31,15) ≈ 3×10^8、累加總和 2^31 ≈ 2.1×10^9，都超過 int，要用 long long。`,
    t: `1. 位元數是 31（因為要小於 2^31），不是 32。用 32 會讓 B_31 算成別的數。
2. 累加的 C(31,k) 總和是 2^31，超過 int，要用 long long（n 本身最大 2147483647 也剛好在 int 邊緣）。
3. 解排名時是「從最高位往低位」走，每一位的選擇數是 C(剩下的位數, 還需要的 1 的個數)。
4. n = 0 要正確回傳 0（k = 0 那一組只有一個元素）。
5. 組合數先打表 C[0..31][0..31]（巴斯卡三角形）比較不容易出錯。
6. 讀到 EOF 為止，沒有測資數也沒有終止符。`,
    c: `#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    const int B = 31;                       // 只考慮小於 2^31 的數
    static long long C[40][40];
    for (int i = 0; i < 40; i++) {
        C[i][0] = 1;
        for (int j = 1; j <= i; j++)
            C[i][j] = C[i - 1][j - 1] + (j <= i - 1 ? C[i - 1][j] : 0);
    }

    long long n;
    while (cin >> n) {
        // 第一步：找出 1 的個數 k
        int k = 0;
        long long cum = 0;
        while (cum + C[B][k] <= n) { cum += C[B][k]; k++; }
        long long r = n - cum;              // 在該組中的 0-based 排名

        // 第二步：由高位往低位解排名
        long long ans = 0;
        int need = k;
        for (int b = B - 1; b >= 0; b--) {
            long long zeroCnt = C[b][need];  // 這一位放 0 的方案數
            if (r < zeroCnt) continue;       // 這一位是 0
            r -= zeroCnt;
            ans |= (1LL << b);
            need--;
        }
        cout << ans << "\\n";
    }
    return 0;
}`
  },

  '11583': {
    q: `外星 DNA 是一串「鹼基集合」，每個集合是一個由相異小寫字母組成的字串（每個字母代表一種鹼基）。

你要把這條 DNA 切成「盡量少」的片段，限制是：同一個片段裡的所有鹼基集合，必須至少共用一個相同的鹼基（也就是交集不為空）。

請輸出最少需要「切幾刀」。

輸入：第一行是測資數 t（≤ 100）。每組先一行 n（1 ≤ n ≤ 10000），接著 n 行、每行一個鹼基集合（字母不重複）。
輸出：每組輸出切割次數的最小值。

範例輸入
2
5
as
sd
df
fg
gh
3
plum
orange
plum

範例輸出
2
2`,
    h: `貪心：由左往右掃，維護「目前這個片段的交集」。

    交集 = 全集（用 26 位元遮罩表示，初始為「還沒放任何東西」）
    對每個鹼基集合 s：
        新交集 = 目前交集 ∩ s
        若新交集為空 → 必須在這裡切一刀（答案 +1），並用 s 自己當作新片段的交集
        否則 → 交集 = 新交集

為什麼貪心是對的？因為「片段愈長，交集只會愈小」，所以能不切就不切，一定不會比先切還差（交換論證：把切點往後移不會讓後面的情況變糟）。

用 26 個位元的 bitmask 表示字母集合，交集就是 AND，判斷空集合就是「等於 0」，非常快。

逐步驗算：
  第一組 as, sd, df, fg, gh：
      {a,s} → ∩{s,d} = {s} → ∩{d,f} = {} → 切第 1 刀，重新開始 {d,f}
      {d,f} → ∩{f,g} = {f} → ∩{g,h} = {} → 切第 2 刀，重新開始 {g,h}
      共 2 刀 ✓
  第二組 plum, orange, plum：
      {p,l,u,m} ∩ {o,r,a,n,g,e} = {} → 切第 1 刀
      {o,r,a,n,g,e} ∩ {p,l,u,m} = {} → 切第 2 刀
      共 2 刀 ✓`,
    t: `1. 題目問的是「切幾刀」不是「幾個片段」——片段數 = 刀數 + 1。答案要輸出刀數。
2. 用 26 位元遮罩做交集（AND）最快；n 可到 10000 × 100 組，用 set 會慢很多。
3. 每個字串的字母保證不重複，可以直接 OR 進遮罩。
4. 新片段的交集要設成「當前這個集合」，不是設成全集再跟它取交集（結果一樣，但別忘了設）。
5. n = 1 時答案是 0（不用切）。
6. 每組測資都要重設狀態。`,
    c: `#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int T;
    if (!(cin >> T)) return 0;
    while (T--) {
        int n;
        cin >> n;
        int cur = 0;                       // 目前片段的交集（26 位元遮罩）
        bool started = false;
        int cuts = 0;
        for (int i = 0; i < n; i++) {
            string s;
            cin >> s;
            int mask = 0;
            for (size_t j = 0; j < s.size(); j++) mask |= 1 << (s[j] - 'a');
            if (!started) { cur = mask; started = true; continue; }
            if ((cur & mask) == 0) { cuts++; cur = mask; }   // 交集空了，切一刀
            else cur &= mask;
        }
        cout << cuts << "\\n";
    }
    return 0;
}`
  },

  '11024': {
    q: `鎖是一個 2×2 的「模數計數器」陣列。第 i 列第 j 行的計數器週期是 p_ij，目前狀態是 s_ij（0 ≤ s_ij < p_ij）。按一下計數器，狀態從 i 變成 i+1，到 p−1 時再按會回到 0。

面板上每一列、每一行各有一個「增加鈕」：按下某一列的鈕，該列的兩個計數器各被觸發一次；按下某一行的鈕同理。當四個計數器全部變成 0 時鎖就打開。

教授還發現一個關係：週期為 p11, p12, p21, p22、狀態為 s_ij 的鎖能被打開，若且唯若「所有週期都等於 p = gcd(p11,p12,p21,p22)、狀態為 s_ij mod p」的鎖能被打開。

輸入：第一行是情境數 n。每個情境兩行、每行四個整數：第 i 行依序是 s_i1, s_i2, p_i1, p_i2（週期 ≤ 10000）。
輸出：能打開印「Yes」，否則印「No」。

範例輸入
2
0 11 17 13
5 8 6 11
1 1 3 3
1 0 3 3

範例輸出
Yes
No`,
    h: `照題目給的關係先化簡：令 p = gcd(p11, p12, p21, p22)，把所有狀態改成 s_ij mod p，這樣四個計數器就有共同的模數 p。

設列鈕分別按了 r1、r2 次，行鈕按了 c1、c2 次。第 (i,j) 個計數器最後的狀態是

    s_ij + r_i + c_j ≡ 0 (mod p)

四條式子做「交錯相加減」把 r、c 全部消掉：

    (s11 + r1 + c1) − (s12 + r1 + c2) − (s21 + r2 + c1) + (s22 + r2 + c2)
        = s11 − s12 − s21 + s22

所以「可以打開」的必要條件是

    s11 − s12 − s21 + s22 ≡ 0 (mod p)

而且這個條件也是充分的：取 r1 = 0、c1 = −s11、c2 = −s12、r2 = s11 − s21，前三個計數器自動歸零，第四個歸零的條件正好就是上式。

所以整題就是一行判斷式。

【驗算】
  第一組：p = gcd(17, 13, 6, 11) = 1。模 1 之下任何數都是 0 → 條件成立 → Yes ✓
  第二組：p = gcd(3,3,3,3) = 3，s11=1, s12=1, s21=1, s22=0
      1 − 1 − 1 + 0 = −1 ≡ 2 (mod 3) ≠ 0 → No ✓
兩組都對上。`,
    t: `1. 輸入每一行是「s_i1 s_i2 p_i1 p_i2」——狀態在前、週期在後，四個數字的順序不要弄反。
2. 化簡到共同模數 p = gcd(四個週期) 是題目直接給的關係，可以放心用；不化簡的話直接推會比較麻煩。
3. p = 1 時答案永遠是 Yes（模 1 之下什麼都是 0），範例第一組就是這種情形。
4. 交錯和可能是負的，取模之前要先加上 p 的倍數：((x % p) + p) % p。
5. gcd 要對四個數一起取（gcd(gcd(a,b), gcd(c,d))）。
6. 輸出是「Yes」/「No」，首字母大寫。`,
    c: `#include <bits/stdc++.h>
using namespace std;

long long gcdll(long long a, long long b) { while (b) { long long t = a % b; a = b; b = t; } return a; }

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int T;
    if (!(cin >> T)) return 0;
    while (T--) {
        long long s11, s12, p11, p12, s21, s22, p21, p22;
        cin >> s11 >> s12 >> p11 >> p12;
        cin >> s21 >> s22 >> p21 >> p22;
        long long p = gcdll(gcdll(p11, p12), gcdll(p21, p22));
        // 交錯和必須是 p 的倍數
        long long v = ((s11 - s12 - s21 + s22) % p + p) % p;
        cout << (v == 0 ? "Yes" : "No") << "\\n";
    }
    return 0;
}`
  },

  '11166': {
    q: `一般的二進位表示法是把非負整數寫成 Σ a_i·2^i，其中每個 a_i 是 0 或 1。這題考慮「有號二進位表示法」：仍然寫成 Σ a_i·2^i，但 a_i 可以是 −1、0 或 1。

例如 n = 13 可以寫成 (1, 0, 0, −1, −1) = 2^4 − 2^1 − 2^0。

二進位表示法是唯一的，但有號的不唯一。在某些應用（例如密碼學）中，我們希望「非零位數愈少愈好」。請找出非零位數最少的表示法；若有多個，輸出「ASCII 字典序最小」的那一個（用 '-' 表示 −1、'0' 表示 0、'+' 表示 +1，不能有前導零）。

輸入：多行（最多 25 組），每行是一個以二進位寫出的正整數 n（≤ 250000，沒有前導零）。以 n = 0 結束。
輸出：每行輸出對應的有號二進位表示法。

範例輸入
10000
1111
10111
0

範例輸出
+0000
+000-
++00-`,
    h: `【先做 DP 求最少非零位數】
從最低位往高位處理，狀態是「目前的進位 c ∈ {0,1}」。在第 i 位，令 b = bit_i(n) + c：
    b = 0 → 這一位只能是 0，新進位 0
    b = 1 → 這一位可以是 +1（新進位 0）或 −1（新進位 1）
    b = 2 → 這一位只能是 0，新進位 1
（可以驗證進位永遠落在 {0,1}。）

    dp[i][c] = 從第 i 位往上、帶著進位 c 所需的最少非零位數
    邊界：dp[最高位之上][0] = 0

【再挑字典序最小的那個】
關鍵是 ASCII 順序：'+' = 43 < '-' = 45 < '0' = 48。所以 '+' 最小、'0' 最大。

注意「字串長度不同也要比」——範例第三組就是重點：
    n = 23 的兩個最少非零（都是 3 個）表示法是
        "++00-"  （16 + 8 − 1，5 位）
        "+0-00-" （32 − 8 − 1，6 位，這是常見的 NAF 形式）
    逐字比較：第 0 位都是 '+'；第 1 位 '+'(43) < '0'(48) → "++00-" 較小 ✓
  所以「NAF（非相鄰形式）」雖然也是最少非零，卻不一定是答案。

作法：沿著 DP 的「最佳轉移」把所有最少非零的表示法列舉出來（去掉前導零後）取字串最小值即可。我實測 n ≤ 250000 時最佳表示法的個數很少（範例中最多只有 2 個），列舉完全不會爆。

我用這套流程跑三組範例，得到 +0000 / +000- / ++00-，與題目輸出一字不差。`,
    t: `1. 不要直接輸出 NAF！NAF 保證非零位數最少，但不保證字典序最小——範例第三組就是反例。
2. ASCII 順序是 '+' < '-' < '0'，跟直覺的「0 最小」相反。
3. 比較時字串長度可能不同，要用一般的字串字典序比較（短的若是長的前綴則短的較小），不能補零成等長再比。
4. 輸出不能有前導零，所以列舉完要把開頭的 '0' 去掉再比較。
5. 進位只會是 0 或 1，狀態很小；但別忘了最高位之上可能還要再進一位（例如 1111 → +000-）。
6. 終止條件是 n = 0（那一行不處理）。`,
    c: `#include <bits/stdc++.h>
using namespace std;

const int MAXB = 25;                 // n <= 250000 < 2^18，留足餘裕
int bitv[MAXB + 1];
int dp[MAXB + 2][2];
string best;

void rec(int i, int c, string& acc) {
    if (i == MAXB) {
        if (c != 0) return;
        string s(acc.rbegin(), acc.rend());          // acc 是由低位往高位累積
        size_t p = s.find_first_not_of('0');
        s = (p == string::npos) ? "0" : s.substr(p); // 去掉前導零
        if (best.empty() || s < best) best = s;
        return;
    }
    int b = bitv[i] + c;
    if (b % 2 == 0) {
        int nc = b / 2;
        if (nc < 2 && dp[i + 1][nc] == dp[i][c]) { acc += '0'; rec(i + 1, nc, acc); acc.erase(acc.size() - 1); }
    } else {
        int nc = (b - 1) / 2;
        if (nc < 2 && dp[i + 1][nc] + 1 == dp[i][c]) { acc += '+'; rec(i + 1, nc, acc); acc.erase(acc.size() - 1); }
        nc = (b + 1) / 2;
        if (nc < 2 && dp[i + 1][nc] + 1 == dp[i][c]) { acc += '-'; rec(i + 1, nc, acc); acc.erase(acc.size() - 1); }
    }
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    string s;
    while (cin >> s) {
        long long n = 0;
        for (size_t i = 0; i < s.size(); i++) n = n * 2 + (s[i] - '0');
        if (n == 0) break;

        for (int i = 0; i < MAXB; i++) bitv[i] = (int)((n >> i) & 1);
        const int INF = 1000000;
        for (int c = 0; c < 2; c++) dp[MAXB][c] = (c == 0) ? 0 : INF;
        for (int i = MAXB - 1; i >= 0; i--)
            for (int c = 0; c < 2; c++) {
                int b = bitv[i] + c, r = INF;
                if (b % 2 == 0) { int nc = b / 2; if (nc < 2) r = min(r, dp[i + 1][nc]); }
                else {
                    int nc = (b - 1) / 2; if (nc < 2) r = min(r, dp[i + 1][nc] + 1);
                    nc = (b + 1) / 2;     if (nc < 2) r = min(r, dp[i + 1][nc] + 1);
                }
                dp[i][c] = r;
            }

        best.clear();
        string acc;
        rec(0, 0, acc);
        cout << best << "\\n";
    }
    return 0;
}`
  }
};
