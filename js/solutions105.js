/* 第六十五批 —— pdftotext 重抽題敘後補回 */
const SOL105 = {
  '10372': {
    q: `超人不會飛，他是用「跳」的越過高樓。因為他隨時要去抓壞人、無法登記飛行路線，為了避免撞到飛機，他希望**跳躍的最高點越低越好**。給你一個城市剖面，求出讓最高點最低的「起跳角度與初速」。

重力加速度是 9.8 m/s²（向下），垂直位移的公式是 d(t) = vt + 0.5at²。

輸入：一連串城市剖面，每個的格式是
    n
    0  d1
    h2 d2
    …
    h(n−1) d(n−1)
    0  dn
超人從地面起跳、跳過 d1 + … + dn 公尺後落回地面，途中必須越過高度 h2 到 h(n−1) 的建築物（各自的寬度就是對應的 d）。n 至多 100。
輸出：讓最高點最低的角度與初速，兩個數印在同一行、各取兩位小數；容許 0.01 度或 0.01 m/s 的誤差。

範例輸入
3
0 5
10 5
0 5
5
0 10.5
20 11.5
25 10
10 15
0 7

範例輸出
71.57 15.65
67.07 27.16`,
    h: `【先把軌跡參數化】
從地面 (0,0) 起跳、落回地面 (D,0)，D = Σdᵢ。拋物線一定是**對稱**的，頂點在 x = D/2，
所以整條軌跡只由「最高點高度 H」決定：

    y(x) = 4H · x(D − x) / D²

【建築物的限制化成 H 的下界】
第 i 棟建築物高 hᵢ、佔據 x ∈ [X_{i−1}, Xᵢ]（X 是 d 的前綴和）。
因為拋物線是**凹的**，在區間上的最小值必定發生在**端點**，所以只要檢查兩個端點就夠：

    y(x) ≥ hᵢ  ⟹  H ≥ hᵢ · D² / (4x(D − x))

把所有建築物的兩個端點都算一遍，取最大值就是 H_min。整題只要一個 max 迴圈，沒有搜尋。

【由 H 反推角度與速度】
    v_y = √(2gH)、上升時間 t = v_y/g、全程時間 T = 2t、v_x = D/T
    角度 = atan(v_y / v_x)、初速 = √(v_x² + v_y²)
順帶一提，v_y/v_x = 4H/D，**與重力加速度無關**，所以角度只由 H 與 D 決定。

【逐組驗算】（兩組範例都手算過）
  第 1 組：D = 15，唯一的建築物高 10、佔 [5, 10]。
      H ≥ 10 × 225 / (4 × 5 × 10) = **11.25**
      4H/D = 3 → 角度 = atan 3 = **71.5651°** → 71.57 ✓
      速度 = √245 = **15.6525** → 15.65 ✓
  第 2 組：D = 54，三棟建築物 (20, [10.5,22])、(25, [22,32])、(10, [32,47])。
      最緊的限制來自**第一棟的左緣** x = 10.5：H ≥ 20 × 2916 / 1827 = **31.9212**
      角度 = atan(4H/D) = atan(2.36453) = **67.0757°**、速度 = **27.1580**
      題目輸出寫的是 67.07（我的精確值 67.0757 四捨五入是 67.08），
      兩者相差 0.0057，落在題目允許的 0.01 度誤差內，所以是特判可接受的範圍。`,
    t: `1. 起跳與落地都在地面，所以拋物線一定對稱、頂點在 D/2；整條軌跡只由最高點 H 決定，不必同時解角度與速度。
2. 拋物線是凹的，所以「在整段區間上高於屋頂」只要檢查**兩個端點**即可，不用細分。
3. 最緊的限制常常來自建築物**靠近起跳點那一側的邊緣**（第二組就是第一棟的左緣），只檢查右緣會低估 H。
4. 第一行與最後一行的 h 是 0，不產生限制，但它們的 d 仍要算進總長度 D。
5. 角度與 g 無關（v_y/v_x = 4H/D），可以拿來檢查自己的推導。
6. 兩位小數；題目允許 0.01 的誤差，所以最後一位與題目範例差一並不代表算錯（第二組就是這種情形）。
7. 讀到 EOF 結束。`,
    c: `#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout << fixed << setprecision(2);
    const double G = 9.8;
    int n;
    while (cin >> n) {
        vector<double> h(n), d(n);
        for (int i = 0; i < n; i++) cin >> h[i] >> d[i];
        vector<double> X(n + 1, 0);
        for (int i = 0; i < n; i++) X[i + 1] = X[i] + d[i];
        double D = X[n];

        double H = 0;
        for (int i = 0; i < n; i++) {
            if (h[i] <= 0) continue;
            for (int k = 0; k < 2; k++) {                 // 兩個端點都要檢查
                double x = X[i + k];
                if (x <= 0 || x >= D) continue;
                H = max(H, h[i] * D * D / (4.0 * x * (D - x)));
            }
        }

        double vy = sqrt(2.0 * G * H);
        double T = 2.0 * vy / G;
        double vx = D / T;
        cout << atan2(vy, vx) * 180.0 / acos(-1.0) << " " << sqrt(vx * vx + vy * vy) << "\n";
    }
    return 0;
}`
  },

  '11703': {
    q: `一個數列由下列遞迴式定義：

    x₀ = 1
    xᵢ = x_{i − ⌊√i⌋} + x_{⌊ln i⌋} + x_{⌊i·sin²(i)⌋}

輸入：若干行，每行一個整數 i（0 ≤ i ≤ 1000000）。最後一行是「-1」，代表結束、不需處理。
輸出：對每個 i 輸出 xᵢ 對 1000000 取模的結果。

範例輸入
0
-1

範例輸出
1`,
    h: `【三個下標都嚴格小於 i】
    i − ⌊√i⌋ < i         （i ≥ 1 時 ⌊√i⌋ ≥ 1）
    ⌊ln i⌋ < i           （i = 1 時 ln 1 = 0）
    ⌊i·sin²(i)⌋ < i      （整數 i ≠ 0 時 sin²(i) 嚴格小於 1）
所以整個數列可以由小到大**一次遞推算完**，不需要記憶化搜尋，也不會有循環相依。

【做法】
開一個長度 1000001 的陣列，x[0] = 1，然後

    for i = 1 to 1000000:
        x[i] = ( x[i − (int)sqrt(i)] + x[(int)log(i)] + x[(int)(i * sin(i) * sin(i))] ) mod 1000000

全部預先算好之後，每個查詢都是 O(1)。一百萬項只需要幾個浮點運算，非常快。

【浮點的小地方】
    sqrt 與 log 在邊界可能因為誤差差一（例如 sqrt(x·x) 算出 x − 1e−9），
    保險做法是算完之後校正一次（往上/往下試一格）。
    sin 的單位是**弧度**，直接用整數 i 當引數，不要轉成角度。
    i·sin²(i) 要用 double 算再取整，i 到一百萬時精度仍然足夠。

【範例】i = 0 → x₀ = 1 ✓（範例只給了這一組，剛好是基底情形）。

【模數】是 1000000（一百萬，六個零），不是常見的 10⁹+7，很容易看錯。
因為只有加法，用 int 就夠，但每一步都要取模。`,
    t: `1.【這題我標了不確定】原題 PDF 的下標在抽取時被壓爛了（顯示成「xi-i + xln(i) + xi sin2(i)」，根號與高斯符號都不見了）。我依標題「sqrt log sin」與殘留的片段還原成 x_{i−⌊√i⌋} + x_{⌊ln i⌋} + x_{⌊i·sin²(i)⌋}。範例只給了 i = 0（答案 1），無法驗證遞迴式本身，所以若送出被判 WA，第一個要懷疑的就是這三個下標的形式。
2. 三個下標都嚴格小於 i，所以由小到大遞推即可，不必遞迴。
3. 模數是 1000000，不是 10⁹+7。
4. sin 的引數是弧度，直接放整數 i。
5. sqrt 與 log 取整時要小心浮點誤差差一格，算完後校正一次比較保險。
6. 一百萬項的陣列用 int 即可（模數只有一百萬），記憶體約 4 MB。
7. 讀到 −1 結束，而且 −1 不輸出。`,
    unsure: true,
    c: `#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    const int LIM = 1000000, MOD = 1000000;
    static int x[LIM + 1];
    x[0] = 1;
    for (int i = 1; i <= LIM; i++) {
        int s = (int)sqrt((double)i);                    // 校正浮點誤差
        while ((long long)(s + 1) * (s + 1) <= i) s++;
        while ((long long)s * s > i) s--;
        int a = i - s;

        int b = (int)log((double)i);
        while (exp((double)(b + 1)) <= (double)i) b++;
        while (b > 0 && exp((double)b) > (double)i) b--;

        double sv = sin((double)i);
        int c = (int)((double)i * sv * sv);
        if (c > i - 1) c = i - 1;
        if (c < 0) c = 0;

        x[i] = (int)(((long long)x[a] + x[b] + x[c]) % MOD);
    }
    long long q;
    while (cin >> q && q != -1) cout << x[q] << "\n";
    return 0;
}`
  }
};
