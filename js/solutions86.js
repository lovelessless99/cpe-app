/* 第四十六批 —— pdftotext 重抽題敘後補回 */
const SOL86 = {
  '11490': {
    q: `古代的君王常用一種排兵法嚇唬敵人：把士兵排成有「正方形空洞」的方陣。規則如下：
  (a) 士兵排成列與行。
  (b) 整個陣形的外框是長方形。
  (c) 只有內層可以缺人。
  (d) 缺人的地方必須是「兩塊相等的正方形區域」，所以缺的人數是「某個正整數平方的兩倍」。
  (e) 沿著缺口的水平與垂直方向，士兵的厚度處處相等（角落除外）。

例如 96 個士兵可以排成這種陣形，缺了 2 × 2² = 8 個人，厚度是 3。

給你士兵總數 S，判斷能不能照上述規則排列。

輸入：1000 行，每行一個正整數 S（S < 1000000000000）。以只有一個 0 的一行結束。
輸出：每行輸入產生一行或多行輸出，每行報告一種可能的「缺少人數」。因為數字可能很大，要印它對 100000007 取模的值；若有多種可能，要依「原始數值」由大到小排列（不是取模後的值）。若找不到任何可能，改印「No Solution Possible」。每組輸入的輸出之後印一個空行。

範例輸入
96
102
11100
0

範例輸出
Possible Missing Soldiers = 8

No Solution Possible

Possible Missing Soldiers = 553352
Possible Missing Soldiers = 308898
Possible Missing Soldiers = 45000
Possible Missing Soldiers = 3528`,
    h: `【先把陣形寫成公式】
設厚度是 t、每個正方形洞的邊長是 k。從圖上量出來：

    寬  C = t + k + t + k + t = 3t + 2k
    高  R = t + k + t         = 2t + k

（左右外框各 t、兩個洞中間也是 t；上下外框各 t。）於是

    S = R·C − 2k² = (2t + k)(3t + 2k) − 2k²
      = (6t² + 7tk + 2k²) − 2k²
      = **t · (6t + 7k)**

漂亮地把 2k² 消掉了。

【驗證公式】96 個士兵、t = 3：3(18 + 7k) = 96 → 7k = 14 → k = 2，
所以 R = 8、C = 13、8 × 13 = 104、104 − 2×4 = 96 ✓，缺 8 人 ✓ 與題目的圖完全吻合。

【怎麼枚舉】由 S = t(6t + 7k) 且 k ≥ 1、t ≥ 1，可得 6t² < S，所以

    t 從 1 跑到 √(S/6)（S < 10¹²，所以 t < 約 408248）
    先檢查 t 整除 S；再令 r = S/t − 6t，若 r > 0 且 7 | r，就得到 k = r/7
    缺少人數 = 2k²

【排序】對固定的 S，t 越大 k 越小，所以**由小到大枚舉 t，自然就是缺少人數由大到小**，
不需要額外排序（也正好避開「取模後排序會錯」的陷阱）。

【逐組驗算】（我實作出來跑過三組範例）
  96   → t = 3、k = 2 → 缺 **8** ✓
  102  → t 只能是 1、2、3、6、17…，代進去 r 都不是 7 的倍數 → **No Solution Possible** ✓
  11100 → t = 3 (k=526)、t = 4 (k=393)、t = 10 (k=150)、t = 25 (k=42)
          → 2k² 依序是 553352、308898、45000、3528 ✓ 四行全中`,
    t: `1. 一定要先把幾何寫成公式。S = t(6t + 7k) 之後整題就只剩枚舉，硬去試各種長寬會爆。
2. 排序要依「原始的缺少人數」由大到小，不是取模後的值。由小到大枚舉 t 剛好就是正確順序，不要先取模再排序。
3. k 必須是「嚴格正」的整數（題目說 strictly positive square），k = 0 不算。
4. S 可到 10¹²，要用 long long；6t² 也要小心不要溢位。
5. 迴圈上界是 6t² < S，也就是 t < √(S/6) ≈ 408248，配合「t 必須整除 S」的快速跳過，1000 筆測資也跑得完。
6. 模數是 100000007（不是常見的 1000000007），少一位很容易看錯。
7. 每筆輸入之後都要印一個空行，包含 No Solution Possible 的情況。`,
    c: `#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    const long long MOD = 100000007LL;
    long long S;
    while (cin >> S && S != 0) {
        bool any = false;
        for (long long t = 1; 6 * t * t < S; t++) {      // t 越大 k 越小 -> 天生降冪
            if (S % t) continue;
            long long r = S / t - 6 * t;
            if (r <= 0 || r % 7) continue;
            long long k = r / 7;                          // 洞的邊長，必須 >= 1
            if (k < 1) continue;
            long long miss = (2 * (k % MOD) % MOD) * (k % MOD) % MOD;
            cout << "Possible Missing Soldiers = " << miss << "\n";
            any = true;
        }
        if (!any) cout << "No Solution Possible\n";
        cout << "\n";
    }
    return 0;
}`
  },

  '11691': {
    q: `過敏測試持續數天，每天有兩個動作點：
   1. 每天早上 8 點，最多把「一種」過敏原塗到身上。
   2. 每天晚上 8 點，檢查有沒有過敏反應。

每種過敏原有一個「持續天數」D（整數天）：如果你對它過敏，塗上去之後幾乎立刻開始出現反應，而且恰好會影響接下來 D 次的晚間檢查。

如果檢查時體內同時有兩種以上仍在作用的過敏原，光看反應無法分辨是哪一種造成的。你想找出「最短」的測試計畫，而且計畫必須是非適應性的（事先排定，不能依照先前的檢查結果臨時決定何時塗哪一種）。

輸入：第一行是測資組數 N（N < 30）。每組第一行是過敏原種類數 k（1 ≤ k ≤ 20），接下來 k 行、每行一個整數 D（1 ≤ D ≤ 7）代表該過敏原的持續天數。
輸出：每組一行，輸出最短且能完全判別的非適應性測試計畫所需的天數。（計畫在「體內不再有作用中的過敏原」的那個早上結束，所以只有一種持續 D 天的過敏原時要花 D 天。）

範例輸入
2
3
2
2
2
5
1
4
2
5
2

範例輸出
5
10`,
    h: `【第一步：把「能完全判別」翻譯成一個乾淨的條件】
第 i 種過敏原若在第 s_i 天早上塗上，就會在第 s_i 到 s_i + D_i − 1 天的晚上造成反應。
所以每種過敏原對應一個區間 S_i。若病人對集合 A 中的過敏原過敏，觀察到的反應日就是 ∪_{i∈A} S_i。

能完全判別 ⟺ 所有 2^k 個子集的聯集互不相同。對一族集合來說，這等價於

    **每一個 S_i 都至少有一個「專屬日」——只有它一種過敏原在作用的晚上**

（若某個 S_i 被其他的聯集蓋住，那「全部」與「全部扣掉 i」的觀察結果就一樣，無法分辨；
反過來若每個都有專屬日，任兩個不同子集必定在某個專屬日上有差異。）

【第二步：把排程寫成遞迴式】
把過敏原依開始日排序（每天最多塗一種，所以開始日兩兩相異）：s_1 < s_2 < … < s_k，
令 E_i = s_i + D_i − 1。三個約束：

    (a) s_i ≥ s_{i−1} + 1                （開始日相異且遞增）
    (b) s_i ≥ E_{i−2} + 2                （第 i−1 種要有專屬日：它的專屬區間是
                                          [max(s_{i−1}, E_{i−2}+1), s_i − 1]，必須非空）
    (c) s_i ≥ E_{i−1} − D_i + 2          （第 i 種必須把覆蓋往後延伸，否則它整個被前面蓋住）

對固定的順序，把每個 s_i 都取成上面三者的最大值（也就是最早可行）就是最佳解。
總天數 = E_k。

**約束 (c) 是最容易漏掉的一條。** 我一開始只把它當成「可行性檢查」，
結果 D = (2, 4, 2) 這組會被誤判成無解而繞遠路，答案算成 7；
其實把 s_3 往後推到 5 就可行，最佳解是 6（區間 [1,2]、[2,5]、[5,6]，
專屬日分別是第 1、3、6 天）。要把它當成「抬高 s_i 的下界」才對。

【第三步：DP】
把式子改寫成相對量。令 c_i = s_{i+1} − s_i、g_i = E_{i−1} − s_i（g_1 = −1），則

    c_i = max(1, g_i + 2, D_i − D_{i+1} + 1)
    g_{i+1} = D_i − 1 − c_i          （小於 −1 時可以直接夾成 −1）
    總天數 = Σ_{i=1}^{k−1} c_i + D_k

注意 c_i 需要用到「下一個」的 D，所以狀態要記住目前的 D。於是

    狀態 = (各持續天數還剩幾個, g, 目前的 D)

g 只有 −1 到 5 共 7 種、D 只有 1 到 7 共 7 種，而剩餘數量的組合也很少（k ≤ 20 分給 7 種），
記憶化搜尋瞬間就跑完。

【驗算】
  第 1 組 D = (2,2,2)：c₁ = 1、c₂ = 2、最後加 D₃ = 2 → **5** ✓
  第 2 組 D = (1,4,2,5,2)：→ **10** ✓
另外我寫了一個「枚舉所有開始日組合 + 直接檢查專屬日」的暴力程式，
在 k ≤ 5、D ≤ 5 的 225 組隨機測資上與這個 DP 逐一比對，完全一致（就是靠它抓出上面那個 (c) 的錯）。`,
    t: `1. 「能完全判別」等價於「每種過敏原都有一個只有它在作用的晚上」。想通這一步整題就從指數變成排程問題。
2. 最大的陷阱是約束 (c)：第 i 種必須把覆蓋往後延伸，這是「s_i 的下界」而不是「可行性檢查」。用 D = (2,4,2) 就能驗出差別（正確答案 6，寫錯會得到 7）。
3. 每天早上最多塗一種，所以開始日兩兩相異——不能兩種同一天開始。
4. 只有持續天數的「多重集合」有意義，過敏原的身分不重要，DP 的狀態用各天數剩餘幾個即可。
5. c_i 的公式需要用到下一個過敏原的 D，所以狀態必須帶著「目前的 D」。
6. g 小於 −1 的部分對後面沒有影響，可以夾成 −1 以縮小狀態數。
7. 只有一種過敏原時答案就是 D 本身（Σ 部分是空的）。`,
    c: `#include <bits/stdc++.h>
using namespace std;

int cnt_[8];
map<string, int> memo_;

int go(int g, int dprev, int left) {
    if (left == 0) return dprev;                  // 最後一種貢獻 D_k
    string key;
    for (int d = 1; d <= 7; d++) key += (char)(1 + cnt_[d]);   // 數量可到 20，不能用 0 加
    key += (char)(30 + g);
    key += (char)(60 + dprev);
    map<string, int>::iterator it = memo_.find(key);
    if (it != memo_.end()) return it->second;

    int best = INT_MAX;
    for (int d = 1; d <= 7; d++) {
        if (!cnt_[d]) continue;
        int c = max(1, max(g + 2, dprev - d + 1));    // 三個下界取最大
        int ng = max(-1, dprev - 1 - c);
        cnt_[d]--;
        int v = c + go(ng, d, left - 1);
        cnt_[d]++;
        best = min(best, v);
    }
    memo_[key] = best;
    return best;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int T;
    if (!(cin >> T)) return 0;
    while (T--) {
        int k;
        cin >> k;
        for (int d = 1; d <= 7; d++) cnt_[d] = 0;
        for (int i = 0; i < k; i++) { int d; cin >> d; cnt_[d]++; }
        memo_.clear();
        int best = INT_MAX;
        for (int d = 1; d <= 7; d++) {
            if (!cnt_[d]) continue;
            cnt_[d]--;
            best = min(best, go(-1, d, k - 1));       // 第一種的 g 是 -1
            cnt_[d]++;
        }
        cout << best << "\n";
    }
    return 0;
}`
  }
};
