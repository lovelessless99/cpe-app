/* 第二十九批 —— pdftotext 重抽題敘後補回 */
const SOL69 = {
  '11515': {
    q: `工地上標出了幾個「可以放吊車」的位置，每個位置的吊車手臂長度是 r，會覆蓋以該位置為圓心、半徑 r 的整個圓形區域。

安全規定要求：任兩台吊車的任何部位都不可以碰到彼此。請選擇要在哪些位置放吊車，使「被覆蓋的總面積」最大。

輸入：第一行是測資數。每組第一行是可放置位置數 C（≤ 15），接著 C 行、每行三個整數 x y r（座標介於 −10000 到 10000，r 介於 0 到 10000）。
輸出：每組輸出一個整數 B，使得最大面積 A = B·π。

範例輸入
1
3
0 0 4
5 0 4
-5 0 4

範例輸出
32`,
    h: `因為答案要用「A = B·π」的形式輸出，所以其實不用碰浮點數：

    總面積 = Σ π·r_i² = π · Σ r_i²   →   B = Σ r_i²

所以問題變成：**選一個彼此不衝突的子集合，使 Σ r_i² 最大**。

【衝突條件】兩台吊車 i、j 會碰到彼此 ⟺ 兩圓相交或相切 ⟺
    (xi − xj)² + (yi − yj)² ≤ (ri + rj)²
（題目說「任何部位都不可以碰到」，所以「剛好相切」也算違規，用 ≤。全程整數運算，不會有精度問題。）

【C ≤ 15 → 直接枚舉】
枚舉 2^15 = 32768 個子集合，檢查是否有衝突、算 Σ r_i²，取最大。
更快的寫法是先為每台吊車算出「和它衝突的集合」的位元遮罩 bad[i]，判斷時只要看 (bad[i] & mask) 是否為 0。

【驗算】範例三台吊車：(0,0,4)、(5,0,4)、(−5,0,4)
    1 與 2：距離 5，半徑和 8 → 5 ≤ 8 → 衝突
    1 與 3：距離 5 → 衝突
    2 與 3：距離 10，半徑和 8 → 10 > 8 → 不衝突 ✓
  所以最好的選法是 {2, 3}，B = 4² + 4² = 32 ✓（選 {1} 只有 16）
與題目輸出一致（我也用程式跑過同一組資料）。`,
    t: `1. 輸出的是 B 而不是面積本身——A = B·π，所以 B = Σ r_i²，全程整數，不要用浮點數算面積再除以 π。
2. 「碰到」包含相切，所以衝突條件是「距離 ≤ 半徑和」（用 ≤ 不是 <）。
3. 距離要比平方，別開根號：(dx² + dy²) ≤ (ri + rj)²。座標到 10^4、半徑到 10^4，平方後約 8×10^8，用 long long 保險。
4. r 可以是 0（面積 0 的吊車），它跟別人「相切」也算碰到，仍要納入衝突判斷。
5. C ≤ 15，2^15 的枚舉綽綽有餘；不需要寫最大權獨立集的專用演算法。
6. 空集合也是合法解（B = 0）。`,
    c: `#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int T;
    if (!(cin >> T)) return 0;
    while (T--) {
        int C;
        cin >> C;
        vector<long long> x(C), y(C), r(C);
        for (int i = 0; i < C; i++) cin >> x[i] >> y[i] >> r[i];

        // bad[i]：和第 i 台吊車會互相碰到的集合
        vector<int> bad(C, 0);
        for (int i = 0; i < C; i++)
            for (int j = 0; j < C; j++) {
                if (i == j) continue;
                long long dx = x[i] - x[j], dy = y[i] - y[j], rs = r[i] + r[j];
                if (dx * dx + dy * dy <= rs * rs) bad[i] |= 1 << j;   // 相切也算碰到
            }

        long long best = 0;
        for (int mask = 0; mask < (1 << C); mask++) {
            long long w = 0;
            bool ok = true;
            for (int i = 0; i < C && ok; i++) {
                if (!((mask >> i) & 1)) continue;
                if (bad[i] & mask) { ok = false; break; }
                w += r[i] * r[i];
            }
            if (ok) best = max(best, w);
        }
        cout << best << "\\n";
    }
    return 0;
}`
  },

  '10331': {
    q: `把 Dhaka 看成一張無向圖：地點是節點、道路是邊，每條路有成本。市民往來時「一定走成本最小的路徑」；若有多條最小成本路徑，每一條都會被使用。

一條「路線」就是某兩個地點之間的往來。請找出「被最多路線使用」的那條道路；若有多條並列，全部列出（依道路編號由小到大）。

輸入：多組測資，讀到 EOF。每組第一行是 V E（1 ≤ V ≤ 100），接著 E 行、每行三個整數 s t C，表示 s 與 t 之間有一條成本 C 的道路（s ≠ t）。道路編號依輸入順序從 1 開始。
輸出：每組輸出一行，被使用最多的道路編號（並列時全部列出，用空白分隔）。

範例輸入
4 4
1 2 20
1 4 1
2 3 3
3 4 4
4 4
1 2 1
1 4 1
2 3 1
3 4 1

範例輸出
4
1 2 3 4`,
    h: `V ≤ 100，所以先用 Floyd–Warshall 求出全點對最短距離 d[i][j]，然後對「每條邊」與「每一對地點」逐一檢查。

邊 (u, v, w) 落在 s 到 t 的某條最短路徑上 ⟺

    d[s][u] + w + d[v][t] == d[s][t]    或    d[s][v] + w + d[u][t] == d[s][t]

（兩個方向都要檢查，因為圖是無向的。）

對每條邊統計「有多少對 {s, t}（s < t）滿足上式」，取最大值，輸出所有達到最大值的邊編號。

複雜度：Floyd 是 O(V³) = 10^6；統計是 O(E × V²/2)，V = 100 時 V²/2 ≈ 4950，E 最多也是幾千 → 約 2×10^7，可以接受。

【逐組驗算】（我用程式跑過兩組）
  第一組：邊 1=(1,2,20)、2=(1,4,1)、3=(2,3,3)、4=(3,4,4)
      d(1,2) = 8（走 1-4-3-2，比直達的 20 短）、d(1,3) = 5、d(1,4) = 1、d(2,3) = 3、d(2,4) = 7、d(3,4) = 4
      邊 1：沒有任何最短路徑用到它 → 0
      邊 2：{1,2}, {1,3}, {1,4} → 3
      邊 3：{1,2}, {2,3}, {2,4} → 3
      邊 4：{1,2}, {1,3}, {2,4}, {3,4} → 4  ← 最多
      → 輸出 4 ✓
  第二組：四條邊成本都是 1，構成一個四邊形 1-2-3-4-1
      {1,3} 有兩條最短路（1-2-3 與 1-4-3），{2,4} 也有兩條 → 每條邊各被 3 對用到
      → 四條並列，輸出 1 2 3 4 ✓`,
    t: `1. 「若有多條最小成本路徑，每一條都會被使用」——所以只要邊落在「某一條」最短路徑上就算被用到，不是只算某一條代表路徑。
2. 圖是無向的，判斷式兩個方向都要試（u→v 與 v→u）。
3. 路線是「無序的一對地點」，統計時只跑 s < t，不要重複算兩次。
4. 不連通的點對要跳過（d 仍是無限大）。
5. 可能有重邊（同兩點多條路），Floyd 建表時取最小值，但統計時每條邊都要各自算——所以邊要另外存一份原始清單，不能只留鄰接矩陣。
6. 讀到 EOF 為止；道路編號從 1 開始，依輸入順序。`,
    c: `#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int V, E;
    while (cin >> V >> E) {
        const long long INF = LLONG_MAX / 4;
        vector<vector<long long> > d(V + 1, vector<long long>(V + 1, INF));
        for (int i = 1; i <= V; i++) d[i][i] = 0;
        vector<array<long long, 3> > e(E);
        for (int i = 0; i < E; i++) {
            long long s, t, c;
            cin >> s >> t >> c;
            e[i] = {s, t, c};
            if (c < d[s][t]) { d[s][t] = c; d[t][s] = c; }   // 重邊取小
        }
        for (int k = 1; k <= V; k++)
            for (int i = 1; i <= V; i++) {
                if (d[i][k] >= INF) continue;
                for (int j = 1; j <= V; j++)
                    if (d[i][k] + d[k][j] < d[i][j]) d[i][j] = d[i][k] + d[k][j];
            }

        vector<long long> cnt(E, 0);
        for (int s = 1; s <= V; s++)
            for (int t = s + 1; t <= V; t++) {
                if (d[s][t] >= INF) continue;
                for (int i = 0; i < E; i++) {
                    long long u = e[i][0], v = e[i][1], w = e[i][2];
                    if (d[s][u] + w + d[v][t] == d[s][t] ||
                        d[s][v] + w + d[u][t] == d[s][t]) cnt[i]++;
                }
            }

        long long mx = 0;
        for (int i = 0; i < E; i++) mx = max(mx, cnt[i]);
        bool first = true;
        for (int i = 0; i < E; i++)
            if (cnt[i] == mx) { cout << (first ? "" : " ") << (i + 1); first = false; }
        cout << "\\n";
    }
    return 0;
}`
  }
};
