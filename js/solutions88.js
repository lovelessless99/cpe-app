/* 第四十八批 —— pdftotext 重抽題敘後補回 */
const SOL88 = {
  '10045': {
    q: `科學家在古老電腦上打字，畫面會立刻顯示；但每隔一陣子會憑空多出一些字元，而且多出來的都是他們之前打過的字元。他們推測這台電腦連上了星際網路，多出來的字元其實是外星人送回來的「回音」。他們也猜這套系統的緩衝區有限，所以只要連續打了十個字元還沒收到回音就會停下來等。

請檢查一段畫面內容是否符合這個理論，也就是判斷給定字串是不是一個「緩衝區大小為十的回音字串」。所謂回音字串 e，是把字串 s1 與它完全相同的回音 s2 交錯合併而成（s1 與 s2 各自的字元順序不變）。「緩衝區大小為十」的意思是：s1 中的某個字元，與它在 s2 中的回音之間，相隔不超過九個字元。

因為實驗突然中止，會有三種情況：
    系統在爆炸前已經把所有字元都回音完畢，所以這個字串確實是緩衝區大小為十的回音字串。
    系統還沒把已輸入的字元全部回音完就爆炸了，但到目前為止的行為與理論相符。
    這個字串再怎麼補後續輸出都不可能構成緩衝區大小為十的回音字串，所以科學家一次打太多字，或者他們的理論是錯的。

輸入：第一行是情境數 n。接下來 n 行，每行一個非空字串，只含英文字母與數字，長度不超過 1000。
輸出：每個情境印出對應的一行：
    An echo string with buffer size ten
    Not an echo string, but still consistent with the theory
    Not consistent with the theory

範例輸入
3
ACABCB
ABCAB
aa0123456789b

範例輸出
An echo string with buffer size ten
Not an echo string, but still consistent with the theory
Not consistent with the theory`,
    h: `【先把規則寫成一個佇列】
從左到右掃過整個字串，每個字元只有兩種身分：
    「新打的字」→ 推進待回音佇列的尾端
    「回音」→ 必須等於佇列**最前面**那個字元，然後把它彈出

為什麼回音一定是最前面那個？因為 s2 是 s1 的複製、順序不變，所以配對必然是先進先出。

【關鍵觀察：狀態只有 1024 種】
距離限制「相隔不超過九個字元」代表：某個字元若在位置 p 被打出，它的回音必須落在 p + 10 之前
（含）。反過來說，掃到位置 i 時，**還在等回音的字元一定都是最近 10 個位置打出來的**。

所以只要用一個 10 位元的 bitmask 記錄「最近 10 個位置中，哪些還在等回音」就足以描述狀態：
    bit j = 1 代表位置 (i−1) − j 打出的字元還沒被回音
狀態只有 2¹⁰ = 1024 種，長度 1000 的字串直接 DP，總共 1000 × 1024 × 2 次轉移，瞬殺。

轉移（處理位置 i 的字元 c）：
    當成新打的字：需要 bit 9 是 0（否則位置 i−10 那個字元永遠來不及回音了），
                  新 mask = ((mask << 1) | 1) & 1023
    當成回音：找出最高的設定位 top（也就是最早還在等的那一個），
              它對應位置 (i−1) − top，該位置的字元必須等於 c，
              新 mask = ((mask ^ (1 << top)) << 1) & 1023

【三種答案怎麼判**】
    掃完之後 mask = 0 可達 → 全部回音完畢 → An echo string…
    掃完之後還有狀態可達（但 mask ≠ 0）→ 還沒回音完，但仍相符 → Not an echo string, but still consistent…
    掃完之後沒有任何狀態可達 → Not consistent with the theory

【為什麼「還有殘留」一定算相符】剩下 k 個待回音的字元，只要接下來 k 個位置依序把它們回音掉就行。
若它們在 mask 中的位移由大到小是 j₁ > j₂ > … > j_k，第 m 個會在位置 i+m 被回音，
需要 j_m ≤ 10 − m；而由位移互異遞減可得 j_m ≤ j₁ − (m−1) ≤ 10 − m，**永遠成立**。
所以只要還有狀態存活就一定補得完，不必再檢查。

【逐組驗算】（我實作出來跑過三組範例）
  ACABCB：s1 = ACB（位置 1,2,4）、s2 = ACB（位置 3,5,6）→ 完整回音 ✓
  ABCAB：打了 A、B、C，回音了 A、B，C 還在等 → 尚未完成但相符 ✓
  aa0123456789b：aa 配對掉之後，0 到 9 共十個字元全在等回音；
      此時 b 若當新打的字會超出緩衝區，若當回音又不等於最前面的 0 → 走不下去 ✓`,
    t: `1. 回音一定對應佇列「最前面」的字元（先進先出），不是任意一個還在等的字元。
2. 緩衝區限制轉成 bitmask 是本題的關鍵：待回音的字元必定來自最近 10 個位置，所以狀態只有 1024 種。硬記整個佇列內容會爆炸。
3. 同一個字元可能既能當「新打的字」也能當「回音」，兩條路都要走——所以要用可達狀態集合而不是貪心。
4. 「當成新打的字」之前要檢查 bit 9 是不是 0，否則會讓某個字元永遠來不及回音。
5. 只要掃完後還有任何狀態存活就算「相符」，不需要再驗證能不能補完（上面證明過一定可以）。
6. 字串長度可到 1000，且只含字母與數字（大小寫不同視為不同字元）。
7. 三句輸出要一字不差，注意第二句中間有逗號。`,
    c: `#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int n;
    if (!(cin >> n)) return 0;
    while (n--) {
        string e;
        cin >> e;
        int len = (int)e.size();
        vector<char> cur(1024, 0), nxt(1024, 0);
        cur[0] = 1;                                    // 一開始沒有人在等回音
        for (int i = 0; i < len; i++) {
            fill(nxt.begin(), nxt.end(), 0);
            char c = e[i];
            for (int m = 0; m < 1024; m++) {
                if (!cur[m]) continue;
                if (!(m & 512))                        // 位置 i-10 不能還在等
                    nxt[((m << 1) | 1) & 1023] = 1;    // 當成新打的字
                if (m) {
                    int top = 9;
                    while (!(m & (1 << top))) top--;   // 最早還在等的那一個
                    int pos = (i - 1) - top;
                    if (pos >= 0 && e[pos] == c)
                        nxt[((m ^ (1 << top)) << 1) & 1023] = 1;   // 當成回音
                }
            }
            cur.swap(nxt);
        }
        bool any = false;
        for (int m = 0; m < 1024; m++) if (cur[m]) { any = true; break; }
        if (!any) cout << "Not consistent with the theory\n";
        else if (cur[0]) cout << "An echo string with buffer size ten\n";
        else cout << "Not an echo string, but still consistent with the theory\n";
    }
    return 0;
}`
  },

  '10251': {
    q: `蛋糕上有一朵中心對稱的奶油花，Sumit 想要拿到「剛好一半」的花。請算出在這個前提下，Sumit 最少會拿到多少體積的蛋糕（不含花）。假設：
  a) 用刀子切蛋糕；b) 切痕永遠是一條直線；c) 蛋糕不是圓形就是正方形；d) 切面永遠垂直；
  e) 花一定嚴格位在蛋糕內部；f) 蛋糕高度處處相同；g) 側壁垂直；h) 除了花的位置以外表面都是平的；
  i) 花是各方向對稱的，放在蛋糕的上表面。

輸入：第一行是整數 N，代表測資組數。接下來 N 行，每行三個整數（圓形蛋糕）或四個整數（正方形蛋糕）。
    圓形：L 是半徑、H 是高度、D（D < L）是花心到蛋糕中心的距離。
    正方形：L 是邊長、H 是高度、DX（DX < L/2）是花心到蛋糕中心的水平距離、DY（DY < L/2）是垂直距離。正方形的中心是兩條對角線的交點，邊與座標軸平行。
輸出：每組一行，輸出 Sumit 最少會拿到的蛋糕體積，取到小數點後三位。

範例輸入
3
10 3 8
10 3 0 0
20 1 0 0

範例輸出
49.050
150.000
200.000`,
    h: `【第一步：切線一定通過花心】
花是各方向對稱的，要平分它，切痕（垂直平面）必須通過花的中心。所以問題變成：

    在「所有通過花心 P 的直線」之中，讓分出來的**較小那一塊**面積最小，再乘上高度 H。

【圓形蛋糕：最短弦】
通過 P 的弦之中，最短的那一條垂直於「圓心到 P」的連線，切出來的弓形也最小。
圓心到這條弦的距離就是 D，弓形面積是

    L²·arccos(D/L) − D·√(L² − D²)

驗算第 1 組：L = 10、D = 8 → 100·arccos(0.8) − 8·6 = 64.35011 − 48 = 16.35011，
乘上 H = 3 得 **49.050** ✓

【正方形蛋糕：切角落的三角形】
把正方形放成 [0, L] × [0, L]，花心 P 到「最近的兩條邊」的距離分別是

    px = L/2 − |DX|,   py = L/2 − |DY|

若切痕通過 P 並且與這兩條邊相交，切下來的是一個直角三角形。設它在兩軸上的截距是 a 與 b，
通過 P 的條件是 px/a + py/b = 1，面積是 ab/2。由算幾不等式，
px/a = py/b = 1/2 時面積最小，也就是 a = 2px、b = 2py，於是

    **最小面積 = 2 · px · py**

（在中心時 px = py = L/2，面積是 L²/2，也就是「任何通過中心的直線都平分正方形」，與直覺一致。）

驗算第 2 組：L = 10、DX = DY = 0 → 2 × 5 × 5 = 50，乘 H = 3 得 **150.000** ✓
驗算第 3 組：L = 20 → 2 × 10 × 10 = 200，乘 H = 1 得 **200.000** ✓

【這個公式對嗎】我寫了一個「枚舉 20 萬個角度、用多邊形裁切算兩塊面積取小」的暴力程式，
拿 (L, DX, DY) = (10,0,0)、(20,0,0)、(10,3,1)、(10,4.5,4)、(10,4,0)、(10,1,4.8)、(7,2,3)
七組去比對，**每一組都與 2·px·py 完全相同**——包括花心貼近邊、貼近角落等各種位置。
（順帶一提，花心落在某條邊的中垂線上時，「切角落的三角形」與「平行近邊切一條」面積剛好相等，
所以不必特別處理梯形的情形。）`,
    t: `1. 切痕必須通過花心——這是把題目從「二維最佳化」降成「一參數最佳化」的關鍵，因為花是對稱的。
2. 圓形取的是「最短弦」，也就是垂直於圓心–花心連線的那一條；不是通過圓心的直徑（那會給出最大的一半）。
3. 弓形面積公式是 R²·arccos(d/R) − d·√(R² − d²)，別漏掉後面那一項。
4. 正方形的答案是 2·px·py，其中 px、py 是花心到「最近的兩條邊」的距離，所以 DX、DY 要先取絕對值。
5. 輸入每行的整數個數不一樣（三個是圓形、四個是正方形），要整行讀進來再數 token，不能直接用固定次數的 cin >>。
6. 答案是「面積 × 高度 H」，別忘了乘 H。
7. 輸出固定三位小數。`,
    c: `#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout << fixed << setprecision(3);
    int n;
    if (!(cin >> n)) return 0;
    string line;
    getline(cin, line);
    while (n--) {
        if (!getline(cin, line)) break;
        istringstream in(line);
        vector<double> v;
        double t;
        while (in >> t) v.push_back(t);
        if (v.size() < 3) { n++; continue; }            // 空行，跳過

        double area;
        if (v.size() == 3) {                            // 圓形：最短弦切出的弓形
            double L = v[0], D = v[2];
            area = L * L * acos(D / L) - D * sqrt(L * L - D * D);
        } else {                                        // 正方形：切最近角落的三角形
            double L = v[0], dx = fabs(v[2]), dy = fabs(v[3]);
            double px = L / 2.0 - dx, py = L / 2.0 - dy;
            area = 2.0 * px * py;
        }
        cout << area * v[1] << "\n";                   // 乘上高度
    }
    return 0;
}`
  },

  '11841': {
    q: `Willy 用白棋、Benny 用黑棋，在「n 階三角格點」上玩 Y 遊戲。n 階格點共有 (n+2)(n+1)/2 個點，每個點有非負的重心座標 (x, y, z) 且 x + y + z = n。若一個點的 x = 0（分別是 y = 0、z = 0），就說它位在 x 邊（y 邊、z 邊）上。

遊戲規則很複雜，但結束時每個格點上都會有一顆棋子。獲勝者是「形成 Y 的人」——也就是他的棋子構成一個連通集合，而且這個集合在三條邊上各至少有一個點。請判斷已結束的 Y 遊戲的勝者。

輸入：多組測資。每組先是一行兩個整數 n 與 m，n 是格點階數、m 是黑棋（Benny）的數量，0 ≤ n ≤ 20，0 ≤ m ≤ (n+2)(n+1)/2。接著 m 行，每行三個數 x y z，是一顆黑棋的座標（以一個以上的空白分隔）。輸入以一行「0 0」結束。
輸出：依輸入順序，每組印一行，是「Benny」或「Willy」。

範例輸入
3 5
0 1 2
1 0 2
3 0 0
1 1 1
1 2 0
2 3
0 0 2
1 0 1
0 2 0
1 1
1 0 0
0 0

範例輸出
Benny
Willy
Willy`,
    h: `這題敘述很長，但實際上就是一次連通元件搜尋。

【格點與相鄰關係】
所有 x + y + z = n 的非負整數三元組就是格點。因為三個座標和固定，往相鄰格點移動就是
「一個座標 +1、另一個 −1」，所以每個點最多六個鄰居：

    (x±1, y∓1, z)、(x±1, y, z∓1)、(x, y±1, z∓1)

（走完之後三個座標都要 ≥ 0 才是合法格點。）

【判斷勝負】
把黑棋做連通元件搜尋（BFS 或 DFS），若某個元件同時碰到 x = 0、y = 0、z = 0 三條邊，Benny 勝；
否則對白棋做同樣的事。實際上 Y 遊戲有個漂亮的性質：**格點填滿時恰好有一方形成 Y**，
所以只要檢查黑棋，沒有就是 Willy 勝。（保險起見兩邊都檢查也行。）

複雜度 O(n²)，n ≤ 20 只有 231 個點，隨便寫都夠快。

【逐組驗算】（我實作出來跑過三組範例）
  第 1 組 n = 3，黑棋 (0,1,2)、(1,0,2)、(3,0,0)、(1,1,1)、(1,2,0)：
      (0,1,2)–(1,0,2)–(1,1,1)–(1,2,0) 四點相連（每一步都是「一個 +1、一個 −1」），
      其中 (0,1,2) 給 x 邊、(1,0,2) 給 y 邊、(1,2,0) 給 z 邊 → 三邊都有 → **Benny** ✓
  第 2 組 n = 2，黑棋 (0,0,2)、(1,0,1)、(0,2,0)：
      (0,0,2)–(1,0,1) 相連但都沒碰到 z 邊；(0,2,0) 是孤立的 → Benny 沒有 Y。
      白棋是 (2,0,0)、(1,1,0)、(0,1,1)，三點連成一串，
      x 邊來自 (0,1,1)、y 邊來自 (2,0,0)、z 邊來自 (1,1,0) → **Willy** ✓
  第 3 組 n = 1，黑棋只有 (1,0,0)，它的 x = 1 沒碰到 x 邊 → 沒有 Y。
      白棋 (0,1,0) 與 (0,0,1) 相連，x 邊、y 邊、z 邊都有 → **Willy** ✓`,
    t: `1. 相鄰的定義是「一個座標 +1、另一個 −1」，共六個方向；用二維陣列存的話很容易只寫出四個方向。
2. 三條邊的定義是某個座標等於 0，不是「在三角形的外圍」——例如 n = 3 的 (3,0,0) 同時位在 y 邊與 z 邊上。
3. 要找的是「同一個連通元件」同時碰到三條邊，不是「三條邊上各有一顆棋子」。第 2 組的黑棋就是三邊都有棋子但不連通。
4. 輸入只給黑棋，白棋是「其餘所有格點」，要自己補出來。
5. 結束條件是「0 0」；雖然題目說 n 可以是 0，但 0 0 一律當成結束。
6. m 可以是 0（完全沒有黑棋），這時 Willy 必勝（n ≥ 1 時白棋佔滿整個格點）。
7. 座標之間可能有多個空白，用 cin >> 讀就不必理會。`,
    c: `#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int n, m;
    while (cin >> n >> m) {
        if (n == 0 && m == 0) break;
        vector<vector<char> > black(n + 1, vector<char>(n + 1, 0));
        for (int i = 0; i < m; i++) {
            int x, y, z;
            cin >> x >> y >> z;
            black[x][y] = 1;                              // z 由 n-x-y 決定
        }

        // 六個鄰居：一個座標 +1、另一個 -1
        const int dx[6] = { 1, -1, 1, -1, 0, 0 };
        const int dy[6] = { -1, 1, 0, 0, 1, -1 };
        vector<vector<char> > seen(n + 1, vector<char>(n + 1, 0));
        bool bennyWins = false;
        for (int x = 0; x <= n && !bennyWins; x++)
            for (int y = 0; y <= n - x && !bennyWins; y++) {
                if (!black[x][y] || seen[x][y]) continue;
                bool sx = false, sy = false, sz = false;
                vector<pair<int, int> > st;
                st.push_back(make_pair(x, y));
                seen[x][y] = 1;
                while (!st.empty()) {
                    pair<int, int> p = st.back(); st.pop_back();
                    int a = p.first, b = p.second, c = n - a - b;
                    if (a == 0) sx = true;
                    if (b == 0) sy = true;
                    if (c == 0) sz = true;
                    for (int d = 0; d < 6; d++) {
                        int na = a + dx[d], nb = b + dy[d];
                        if (na < 0 || nb < 0 || na + nb > n) continue;
                        if (!black[na][nb] || seen[na][nb]) continue;
                        seen[na][nb] = 1;
                        st.push_back(make_pair(na, nb));
                    }
                }
                if (sx && sy && sz) bennyWins = true;     // 同一個元件碰到三條邊
            }
        cout << (bennyWins ? "Benny" : "Willy") << "\n";
    }
    return 0;
}`
  }
};
