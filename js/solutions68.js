/* 第二十八批 —— pdftotext 重抽題敘後補回 */
const SOL68 = {
  '11655': {
    q: `Waterland 有 L 座島，島 1 是總統辦公室（Kurai），島 L 是渡假地（Yablonoi）。安全部隊挑出了 T 條「安全隧道」，全是單向的，而且保證「從島 1 走到島 L 的任何路線都不會重複經過同一座島」（也就是這些隧道構成一個有向無環圖）。

安全部隊會在「每一條從島 1 到島 L 的路線」上各派出一組隊伍。每組沿途在「經過的每一座島（島 1 除外）」各留下一支隊伍後繼續前進，所以每一組最後只有一支隊伍抵達島 L。

請算出：整個過程總共需要幾支隊伍（S），以及最後有幾支隊伍抵達島 L（D）。兩者都對 100000 取模。

輸入：第一行是測資數 N（≤ 40）。每組第一行是 L T（2 ≤ L ≤ 5000，0 ≤ T ≤ 5L），接著 T 行、每行兩個整數 x y 表示一條 x → y 的單向隧道。任兩島之間最多一條隧道。
輸出：每組印「Case i: S D」。

範例輸入
3
2 1
1 2
4 4
1 2
1 3
2 4
3 4
4 3
1 2
2 3
3 4

範例輸出
Case 1: 1 1
Case 2: 4 2
Case 3: 3 1`,
    h: `圖保證是 DAG，所以用拓撲排序 + 兩個方向的路徑計數就好。

    f[v] = 從島 1 走到 v 的「路線數」
    g[v] = 從 v 走到島 L 的「路線數」

【D：抵達終點的隊伍數】
每一條路線最後恰好送到一支隊伍，所以

    D = f[L]（= g[1]，兩者相等）

【S：總共需要的隊伍數】
一支隊伍被留在島 v（v ≠ 1）⟺ 有一條路線經過 v。而「經過 v 的路線數」正好是

    f[v] × g[v]

（前半段從 1 到 v 有 f[v] 種、後半段從 v 到 L 有 g[v] 種，互相獨立。）所以

    S = Σ_{v ≠ 1} f[v] × g[v]

【逐組驗算】（我用程式跑過三組）
  第一組（1→2）：唯一路線長度 1 → f[2]·g[2] = 1 → S = 1、D = 1 ✓
  第二組（1→2, 1→3, 2→4, 3→4）：兩條路線 1-2-4 與 1-3-4
      v=2：1×1 = 1；v=3：1×1 = 1；v=4：f=2, g=1 → 2
      S = 4、D = 2 ✓
  第三組（1→2→3→4）：一條長度 3 的路線 → v=2,3,4 各 1 → S = 3、D = 1 ✓
三組全中。`,
    t: `1. 兩個數都要對 100000 取模（不是質數，只是單純取餘數）。
2. S 是「總隊伍數」不是「總邊數」——雖然這三組範例剛好相等（因為每條路線經過的島數等於邊數），但一般情況下要用 Σ f[v]·g[v] 才對。
3. 島 1 不留隊伍，加總時要從 v = 2 開始。
4. 圖可能不連通、也可能有到不了 L 的島，這些島的 f 或 g 是 0，自然不會貢獻。
5. L 可到 5000、T 到 25000，用拓撲排序（Kahn）+ 一次正向、一次反向的 DP，O(L + T)。
6. 設初始值時要小心：f[1] = 1、g[L] = 1，累加時要跳過這兩個起點本身，避免被自己的入邊/出邊覆蓋。`,
    c: `#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    const long long MOD = 100000;
    int T;
    if (!(cin >> T)) return 0;
    for (int tc = 1; tc <= T; tc++) {
        int L, m;
        cin >> L >> m;
        vector<vector<int> > adj(L + 1);
        vector<int> indeg(L + 1, 0);
        for (int i = 0; i < m; i++) {
            int x, y;
            cin >> x >> y;
            adj[x].push_back(y);
            indeg[y]++;
        }

        // 拓撲排序
        vector<int> order_;
        queue<int> q;
        for (int i = 1; i <= L; i++) if (indeg[i] == 0) q.push(i);
        while (!q.empty()) {
            int u = q.front(); q.pop();
            order_.push_back(u);
            for (size_t k = 0; k < adj[u].size(); k++)
                if (--indeg[adj[u][k]] == 0) q.push(adj[u][k]);
        }

        vector<long long> f(L + 1, 0), g(L + 1, 0);
        f[1] = 1;
        for (size_t i = 0; i < order_.size(); i++) {
            int u = order_[i];
            for (size_t k = 0; k < adj[u].size(); k++) {
                int v = adj[u][k];
                if (v == 1) continue;                     // 起點不被累加覆蓋
                f[v] = (f[v] + f[u]) % MOD;
            }
        }
        g[L] = 1;
        for (int i = (int)order_.size() - 1; i >= 0; i--) {
            int u = order_[i];
            if (u == L) continue;                         // 終點固定為 1
            long long s = 0;
            for (size_t k = 0; k < adj[u].size(); k++) s = (s + g[adj[u][k]]) % MOD;
            g[u] = s;
        }

        long long S = 0;
        for (int v = 2; v <= L; v++) S = (S + f[v] * g[v]) % MOD;
        cout << "Case " << tc << ": " << S << " " << f[L] % MOD << "\\n";
    }
    return 0;
}`
  },

  '11632': {
    q: `Elias gamma 編碼把一個正整數 n 編成：設 n 的二進位有 k 個位元，先寫 k−1 個 0 再寫一個 1（這叫「前綴」，長度 k），然後寫出 n 的二進位。（本題稍作修改，讓 0 也能編碼：0 的二進位是「0」，1 個位元。）

    數字  二進位  位元數  前綴  編碼
    0     0       1       1     10
    2     10      2       01    0110
    8     1000    4       0001  00011000

一串整數就是把各自的編碼接起來。要縮短總長度，可以做兩種最佳化：
  1. 若某個「表示 k 個位元」的前綴其實沒有任何整數用到，就可以把它拿來表示 k+1 個位元；原本表示 k+1 的前綴又可以往上遞補，依此類推。
  2. 可以把「所有 k 位元的整數」前面補一個 0，讓它們變成 k+1 位元，然後再套用第 1 種最佳化。

輸入：多組測資。每組第一行是 n（1 ≤ n ≤ 128），第二行是 c1, ..., cn（0 ≤ ci ≤ 10000），其中 ci 表示「恰好有 i 個位元的整數有幾個」。以 n = 0 結束。
輸出：每組輸出一行最短的編碼總長度。

範例輸入
4
2 4 0 1
5
9 4 2 4 3
11
44 56 96 26 73 80 77 50 33 16 78
0

範例輸出
24
99
5494`,
    h: `先看清楚兩種最佳化合起來會變成什麼結構：

  - 最佳化 2 把「第 i 組」整體往上併到第 i+1 組，可以連續做很多次；
  - 所以最後的結果一定是：把位元數 1..n 切成若干「連續區間」，每個區間裡的整數全部變成「該區間右端點」的位元數。
  - 最佳化 1 則說：實際被用到的位元長度由小到大排序後，第 j 個用到的長度，前綴長度就是 j。

於是總長度 =  Σ over 各個非空區間（依序編號 j = 1, 2, ...）of （區間內的整數個數）×（j + 區間右端點）

用 DP 求最小值：
    dp[i][j] = 位元數 1..i 都處理完、已經用掉 j 個前綴的最小總長度
    轉移：選下一個區間 (i, b]
        S = c_{i+1} + ... + c_b
        若 S > 0：dp[b][j+1] = min(..., dp[i][j] + S × (j + 1 + b))
        若 S = 0：這段沒有整數，不需要前綴 → dp[b][j] = min(..., dp[i][j])
    答案 = min over j of dp[n][j]

n ≤ 128，DP 是 O(n³) ≈ 2×10^6，很快。

【逐步驗算】（題目自己示範的那組：c = 2, 4, 0, 1）
    原始編碼：2×(1+1) + 4×(2+2) + 0 + 1×(4+4) = 4 + 16 + 8 = 28
    只用最佳化 1：用到的長度是 1, 2, 4，前綴編號 1, 2, 3
        → 2×(1+1) + 4×(2+2) + 1×(3+4) = 4 + 16 + 7 = 27
    再用最佳化 2 把 1 位元那組併到 2 位元（變成 6 個 2 位元的數）：
        用到的長度是 2, 4，前綴編號 1, 2
        → 6×(1+2) + 1×(2+4) = 18 + 6 = 24 ✓
我把上面的 DP 實作出來跑三組範例，得到 24 / 99 / 5494，與題目輸出完全一致。`,
    t: `1. 輸入給的是「c_i：恰好 i 個位元的整數有幾個」，不是整數本身。看到範例的 "2 4 0 1" 要想成 c1=2, c2=4, c3=0, c4=1。
2. 最佳化 2 是「整組一起往上併」，不能只把其中幾個往上移——所以最後的結構一定是「連續區間」。
3. 前綴長度取決於「這是第幾個被用到的長度」，不是長度本身。這正是最佳化 1 的效果。
4. 空區間（S = 0）不佔用前綴編號，DP 要特別處理，否則會白白浪費一個較短的前綴。
5. 總長度可能不小（10000 × 128 × 常數），用 long long。
6. 終止條件是 n = 0。`,
    c: `#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int n;
    while (cin >> n && n != 0) {
        vector<long long> c(n + 1, 0), pre(n + 1, 0);
        for (int i = 1; i <= n; i++) { cin >> c[i]; pre[i] = pre[i - 1] + c[i]; }

        const long long INF = LLONG_MAX / 4;
        // dp[i][j]：1..i 都處理完、用掉 j 個前綴
        vector<vector<long long> > dp(n + 1, vector<long long>(n + 2, INF));
        dp[0][0] = 0;
        for (int i = 0; i <= n; i++)
            for (int j = 0; j <= n; j++) {
                if (dp[i][j] >= INF) continue;
                for (int b = i + 1; b <= n; b++) {
                    long long S = pre[b] - pre[i];
                    if (S > 0) {
                        long long cost = dp[i][j] + S * ((long long)(j + 1) + b);
                        if (cost < dp[b][j + 1]) dp[b][j + 1] = cost;
                    } else if (dp[i][j] < dp[b][j]) {
                        dp[b][j] = dp[i][j];               // 空區間不佔前綴
                    }
                }
            }
        long long ans = INF;
        for (int j = 0; j <= n + 1 && j <= n; j++) ans = min(ans, dp[n][j]);
        cout << ans << "\\n";
    }
    return 0;
}`
  }
};
