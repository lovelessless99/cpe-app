/* 第三十九批 —— pdftotext 重抽題敘後補回 */
const SOL79 = {
  '10710': {
    q: `Jimmy 的「完美洗牌」定義如下：手上有 N 張牌（由上到下編號 1 到 N）。把最上面的 ⌊N/2⌋ 張拿在右手、剩下的拿在左手。接著先放下左手最底下那張，再蓋上右手最底下那張，然後又是左手的下一張……左右交替直到所有牌合成一疊。

10 張牌：右手 1 2 3 4 5、左手 6 7 8 9 10 → 1 6 2 7 3 8 4 9 5 10
11 張牌：右手 1 2 3 4 5、左手 6 7 8 9 10 11 → 6 1 7 2 8 3 9 4 10 5 11

9 張牌連續洗牌的結果：
    原始 : 1 2 3 4 5 6 7 8 9
    第 1 次: 5 1 6 2 7 3 8 4 9
    第 2 次: 7 5 3 1 8 6 4 2 9
    第 3 次: 8 7 6 5 4 3 2 1 9
    第 4 次: 4 8 3 7 2 6 1 5 9
    第 5 次: 2 4 6 8 1 3 5 7 9
    第 6 次: 1 2 3 4 5 6 7 8 9
所以 9 張牌要洗 6 次才會復原。N = 3 到 20 的次數依序是 2, 2, 4, 4, 3, 3, 6, 6, 10, 10, 12, 12, 4, 4, 8, 8, 18, 18。

定義「Jimmy 數」：拿 N 張牌，洗 N − 1 次完美洗牌，若牌回到原始順序，N 就是 Jimmy 數；否則不是。

輸入：至多 2000 個整數，每個一行，範圍 54 到 2000000000（含），以 −1 結束（−1 不處理）。
輸出：每個數字一行，印「N is a Jimmy-number」或「N is not a Jimmy-number」。

範例輸入
54
101
144
197
1999999973
1999999975
-1

範例輸出
54 is not a Jimmy-number
101 is a Jimmy-number
144 is not a Jimmy-number
197 is a Jimmy-number
1999999973 is a Jimmy-number
1999999975 is not a Jimmy-number`,
    h: `【先把洗牌寫成一條公式】
拿 N = 9 的例子看「每張牌洗完後跑到第幾個位置」：
    1→2、2→4、3→6、4→8、5→1、6→3、7→5、8→7、9→9
一眼就看出來

    新位置 = 2 × 舊位置 (mod N)        （結果 0 視為 N）

也就是說，奇數張牌時，這個洗牌就是「模 N 乘以 2」。洗 k 次相當於乘 2^k，
所以**復原次數 = 2 在模 N 下的乘法階數**。

偶數 N 呢？看 N = 10：1→1、2→3、3→5、4→7、5→9、6→2、7→4、8→6、9→8、10→10。
最底下那張（第 N 張）永遠不動，其餘 N − 1 張的置換與「模 N − 1 乘 2」共軛。
這就解釋了題目觀察到的「N 是偶數時次數等於 N − 1 的次數」。

【所以判斷式是】
    N 為奇數：Jimmy 數 ⟺ 2^(N−1) ≡ 1 (mod N)
    N 為偶數：令 M = N − 1（奇數），洗牌次數是 2 在模 M 的階數，要能整除 N − 1 = M
              ⟺ 2^M ≡ 1 (mod M)

兩者的「模數」與「指數」搭配不一樣，很容易寫混，要小心。

用快速冪就好，一次 O(log N)，2000 筆瞬殺。

【驗算】我做了三層檢查：
  1. 直接模擬洗牌，重現題目 N = 3 到 20 的次數表 → 18 個數字全對。
  2. 上面的公式對六組範例：54 不是、101 是、144 不是、197 是、1999999973 是、1999999975 不是 → 全中。
  3. 把公式與「暴力模擬求階數」在 N = 54 到 300 全部比對 → 零誤差。

【順帶一提】所有奇質數都是 Jimmy 數（費馬小定理直接給出 2^(N−1) ≡ 1）。
題目提到的 7, 17, 23, 31… 「階數沒有到最大值但仍能復原」就是這個道理；
而合數要成為 Jimmy 數就必須是「以 2 為底的費馬偽質數」（如 341、561），非常稀少。`,
    t: `1. 奇數與偶數的判斷式不同：奇數是 2^(N−1) mod N，偶數是 2^(N−1) mod (N−1)。指數與模數要成對換掉，只換一個就錯。
2. N 可到 2×10^9，兩數相乘會到 4×10^18，超過 signed long long 的安全範圍邊緣——用 unsigned long long 或 __int128 做乘法取模。
3. 快速冪的指數也要用 long long。
4. 結束條件是讀到 −1，而且 −1 不輸出。
5. 輸出字串注意冠詞與連字號：「is a Jimmy-number」/「is not a Jimmy-number」，Jimmy 的 J 大寫。
6. 偶數 N 時模數 M = N − 1 一定是奇數，所以 2 一定可逆，不用擔心退化情形。
7. 至多 2000 筆，不要對每筆去真的模擬洗牌（N 可到 20 億）。`,
    c: `#include <bits/stdc++.h>
using namespace std;

typedef unsigned long long ull;

ull mulmod(ull a, ull b, ull m) { return (unsigned __int128)a * b % m; }

ull powmod(ull b, ull e, ull m) {
    ull r = 1 % m;
    b %= m;
    while (e) {
        if (e & 1ULL) r = mulmod(r, b, m);
        b = mulmod(b, b, m);
        e >>= 1;
    }
    return r;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    long long n;
    while (cin >> n && n != -1) {
        bool jimmy;
        if (n % 2 == 1) jimmy = (powmod(2, (ull)n - 1, (ull)n) == 1);   // 洗牌 = 模 N 乘 2
        else {
            ull m = (ull)n - 1;                                         // 最底下那張不動
            jimmy = (powmod(2, m, m) == 1);
        }
        cout << n << (jimmy ? " is a Jimmy-number" : " is not a Jimmy-number") << "\n";
    }
    return 0;
}`
  },

  '10637': {
    q: `一群數字若「其中任意兩個的最大公因數都是 1」，就稱為互質的一組。請把 S 拆成 t 個互質的數。

輸入：第一行是整數 N（0 < N < 40），代表測資組數。接下來 N 行、每行兩個整數 S（0 < S ≤ 100）與 t（0 < t ≤ 30）。
輸出：每組先印一行編號，接著把「所有」把 S 拆成 t 個互質數的方法各印一行。同一行內的數字要由小到大排列、以單一空白分隔。各行之間依「字典序由小到大」排列（比較第一個不同的位置，較小者排前面）。整份輸出大約會有 15000 行。

範例輸入
2
40 6
8 3

範例輸出
Case 1:
1 1 1 1 1 35
1 1 1 1 5 31
1 1 1 1 7 29
1 1 1 1 11 25
1 1 1 1 13 23
1 1 1 1 17 19
1 1 1 3 5 29
1 1 1 3 11 23
1 1 1 5 9 23
1 1 1 5 11 21
1 1 1 5 13 19
1 1 1 7 11 19
1 1 1 7 13 17
1 1 1 9 11 17
1 1 3 5 7 23
1 1 3 5 11 19
1 1 3 5 13 17
1 1 3 7 11 17
1 1 5 7 9 17
1 1 5 9 11 13
1 3 5 7 11 13
Case 2:
1 1 6
1 2 5
1 3 4`,
    h: `直接 DFS 枚舉「不遞減」的序列即可，而且因為我們是由小到大枚舉每個位置的值，產生出來的順序天生就是字典序，不用另外排序。

    dfs(第 k 個位置, 這個位置的最小值 mn, 還剩下 rem)
        k == t 時：rem == 0 就記錄一組答案
        否則對 v = mn, mn+1, … 嘗試放 v

兩個關鍵：

【互質檢查】新放的 v 只要跟「已經選過的每個大於 1 的數」互質即可。
1 跟誰都互質，所以 **1 可以重複出現任意多次**——範例第一行的「1 1 1 1 1 35」就是。
反過來說，大於 1 的數彼此一定互不相同（相同的數 gcd 就是它自己）。

【剪枝】因為後面的數都 ≥ v，所以剩下的 (t − k) 個位置至少要花掉 v × (t − k)。
    迴圈條件直接寫成 v * (t − k) <= rem
就能把絕大多數分支砍掉，S ≤ 100、t ≤ 30 的規模跑得非常快。

【驗算】我把它實作出來跑範例：
    第一組 S = 40、t = 6 → 產生 21 行，與題目列出的 21 行**逐字完全一致**（順序也一樣）
    第二組 S = 8、t = 3 → 1 1 6 / 1 2 5 / 1 3 4，三行也完全一致 ✓`,
    t: `1. 1 可以重複出現任意多次（gcd(1,1) = 1），千萬別誤以為所有數字都要相異——範例第一行就有五個 1。
2. 反過來，大於 1 的數一定兩兩相異，所以檢查互質時只要跟已選的「大於 1」的數比即可。
3. 由小到大枚舉，輸出天生就是字典序，不需要先蒐集再排序。
4. 剪枝條件 v × (剩餘位置數) ≤ rem 一定要加，否則 t = 30 時會慢到爆。
5. 可能一組解都沒有（例如 S 太小），這時只印「Case k:」而不印任何行。
6. 輸出量可達 15000 行，記得用 ios::sync_with_stdio(false) 之類的快速輸出，別用會每次 flush 的方式。
7. 每行數字以單一空白分隔，行尾不要多餘空白。`,
    c: `#include <bits/stdc++.h>
using namespace std;

int S_, T_;
vector<int> cur_;
ostringstream out_;

int gcd_(int a, int b) { return b ? gcd_(b, a % b) : a; }

void dfs(int k, int mn, int rem) {
    if (k == T_) {
        if (rem == 0) {
            for (int i = 0; i < T_; i++) out_ << (i ? " " : "") << cur_[i];
            out_ << "\n";
        }
        return;
    }
    for (int v = mn; (long long)v * (T_ - k) <= rem; v++) {
        bool ok = true;
        if (v > 1)
            for (size_t i = 0; i < cur_.size() && ok; i++)
                if (cur_[i] > 1 && gcd_(cur_[i], v) != 1) ok = false;
        if (!ok) continue;
        cur_.push_back(v);
        dfs(k + 1, v, rem - v);          // 不遞減，順帶保證輸出是字典序
        cur_.pop_back();
    }
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int n;
    if (!(cin >> n)) return 0;
    for (int t = 1; t <= n; t++) {
        cin >> S_ >> T_;
        out_ << "Case " << t << ":\n";
        cur_.clear();
        dfs(0, 1, S_);
    }
    cout << out_.str();
    return 0;
}`
  }
};
