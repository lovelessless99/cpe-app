/* 第三十七批 —— pdftotext 重抽題敘後補回 */
const SOL77 = {
  '11534': {
    q: `Bob 為 Alice 設計了一個 1×N 格子的兩人遊戲：

    每一步，玩家在一個空格子裡填上「X」或「O」（是大寫字母 O，不是零）。
    不允許出現兩個相鄰的 X，也不允許出現兩個相鄰的 O。
    填下最後一個格子的人獲勝（也就是輪到某人時已經無法落子，該人就輸）。

以 N = 3 為例：若先手在最左格填 X，後手就在最右格填 O，此時中間格左邊是 X、右邊是 O，兩種符號都不能填，先手無法落子而落敗。不過這對先手不是最佳走法——先手若第一手就填中間格（X 或 O 皆可）就會贏。

Alice 永遠是先手。她通常跟 Bob 玩，但 Bob 常常玩到一半就把棋交給電腦接手，而電腦一定走最佳解。給你「Bob 離開時的盤面」，判斷 Alice 有沒有辦法贏。

輸入：第一行是測資數 T（T < 10001）。接下來 T 行，每行一個字串代表盤面，長度 N（0 < N < 101），字元只有「X」「O」與代表空格的「.」。
輸出：每組輸出一行，能贏印「Possible.」，不能贏印「Impossible.」（都有句點）。

範例輸入
3
...
X..
O

範例輸出
Possible.
Impossible.
Possible.`,
    h: `這題有兩層，第二層是很多人卡住的地方。

【第一層：誰是「輪到走的人」】
題目說 Alice 永遠是先手，而給你的是「玩到一半」的盤面。每一步剛好填掉一個空格，所以

    已填格子數 k 是偶數 → 現在輪到 Alice
    k 是奇數           → 現在輪到電腦

範例第二行「X..」就是統計的關鍵：它是 Alice 先填了最左邊的 X 之後的盤面，現在輪到電腦，
而電腦填最右邊的 O 就贏了——所以答案是 Impossible，跟題敘裡的說明完全一致。
第三行「O」也一樣：k = 1，輪到電腦，但電腦無子可落，所以最後一格是 Alice 填的 → Possible。
（如果誤以為「一律輪到 Alice」，這兩行都會答錯。）

【第二層：盤面的 Grundy 值】
落子只影響自己所在的那一段連續空格，所以整個盤面是各段的「獨立遊戲之和」，用 XOR 合併。
把每一段極大的連續空格依「左右兩側是什麼」分成四類（L 是這段的長度，牆代表盤面邊界）：

    牆…牆（整個盤面都是空的）    g = L mod 2
    牆…符號（只有一邊靠牆）      g = L        ← 就是一堆大小為 L 的 Nim 石堆
    符號…符號，兩邊同字母        g = 1
    符號…符號，兩邊不同字母      g = 0        ← 這段其實是死的

    XOR 全部 ≠ 0 ⟺ 輪到走的人獲勝

【怎麼確定的】我寫了暴力搜尋，枚舉長度 0 到 11 的**所有**合法盤面（33460 個），
把「暴力算出的勝負」跟「上面這張表 XOR 出來的結果」逐一比對，完全一致、零誤差。
再套上第一層的回合判斷，題目三組範例也全中。

【直覺】
「符號…符號且兩邊不同」（例如 X..O 中間那段）為什麼是 0？因為兩邊各自禁掉一種字母，
先手怎麼填、後手都能鏡像回應。而「一邊靠牆」之所以是完整的 Nim 堆 L，是因為靠牆那側完全自由。

複雜度 O(N)，T 到 10000 也是瞬殺。`,
    t: `1. 最大的坑：盤面不一定輪到 Alice。數一數已填的格子數，偶數才輪到她；奇數時要反過來判斷。範例的後兩行就是專門在測這件事。
2. 「填下最後一格的人贏」是一般型（normal play），不是 misère——輪到你卻無法落子就是你輸。
3. 四種段落的 Grundy 不一樣，特別注意「整個盤面都空著」是 L mod 2，而「只有一邊靠牆」是 L 本身，兩者完全不同。
4. 兩邊符號不同的段落 Grundy 為 0，可以直接跳過；但仍要正確切段，不能把它跟隔壁合併。
5. 盤面可能完全沒有空格（例如「O」），這時 XOR 是 0，輪到走的人輸。
6. 輸出的字串結尾有句點：「Possible.」「Impossible.」。
7. T 可到 10000，別在每組裡做 O(N²) 以上的事，也記得用快速輸入。`,
    c: `#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int T;
    if (!(cin >> T)) return 0;
    while (T--) {
        string s;
        cin >> s;
        int n = (int)s.size();
        int x = 0, marked = 0;
        for (int i = 0; i < n; i++) if (s[i] != '.') marked++;

        for (int i = 0; i < n; ) {
            if (s[i] != '.') { i++; continue; }
            int j = i;
            while (j < n && s[j] == '.') j++;
            int L = j - i;
            char l = (i == 0) ? 'W' : s[i - 1];
            char r = (j == n) ? 'W' : s[j];
            int g;
            if (l == 'W' && r == 'W') g = L % 2;          // 整盤全空
            else if (l == 'W' || r == 'W') g = L;         // 只有一邊靠牆 = Nim 堆
            else if (l == r) g = 1;                       // 兩邊同字母
            else g = 0;                                   // 兩邊不同字母，死段
            x ^= g;
            i = j;
        }

        bool moverWins = (x != 0);
        bool aliceToMove = (marked % 2 == 0);             // Alice 是先手
        cout << ((moverWins == aliceToMove) ? "Possible." : "Impossible.") << "\n";
    }
    return 0;
}`
  },

  '11004': {
    q: `Texas 的道路都是直線，而且處於「一般位置」：沒有兩條平行、也沒有三條共點。第 i 條路用三個整數 a、b、c 表示，代表直線 ax + by + c = 0。新路的興建計畫如下：

a) 市府選出 P 個重要地點（平面上的點）。新路將從「每一個路口（兩條既有道路的交點）」連到「每一個重要地點」，所以總共會蓋 J × P 條新路，J 是路口數。
b) 設第 i 條與第 j 條路（j > i）的方程式是 aᵢx + bᵢy + cᵢ = 0 與 aⱼx + bⱼy + cⱼ = 0，那麼連接這兩條路的路口與任一重要地點的新路，形式會是
        aᵢx + bᵢy + cᵢ + k(aⱼx + bⱼy + cⱼ) = 0
   k 為正時由 Positive Builders Ltd 承建，k 為負時由 Negative Builders Ltd 承建。

請算出有幾條新路是由 Negative Builders Ltd 承建。

輸入：至多 12 組測資。每組第一行是 N（0 < N ≤ 3000），接著 N 行、每行三個整數 aᵢ bᵢ cᵢ（−10000 ≤ 值 ≤ 10000）。接著一行整數 P（1 ≤ P ≤ 100），再接著 P 行、每行兩個整數 xᵢ yᵢ 是重要地點的座標。保證重要地點不會落在既有道路上。以第一行為 0 的區塊結束。
輸出：每組輸出兩行，第一行是地圖編號，第二行報告 Negative Builders Ltd 要蓋幾條路。格式照範例。

範例輸入
5
-3954 -4700 5677
7190 -5953 3085
-3903 -2020 -7990
-4618 618 -4421
4162 3918 8248
3
3 7
-5 7
-5 -8
6
7697 3593 -4874
3311 2882 -5983
2871 1429 -6858
9436 -8954 -4656
9294 6173 5035
-3924 1911 3733
3
5 -8
1 -10
4 -7
0

範例輸出
Roadmap 1:
Negative Builders Ltd. will build 16 New Roads.
Roadmap 2:
Negative Builders Ltd. will build 23 New Roads.`,
    h: `題目講了一大堆路口，但路口其實根本不用算出來。

設 Lᵢ(x, y) = aᵢx + bᵢy + cᵢ。新路的方程式是 Lᵢ + k·Lⱼ = 0（這是通過 i、j 交點的「直線束」）。
要求它通過重要地點 P，把 P 代進去：

    Lᵢ(P) + k·Lⱼ(P) = 0    ⟹    k = −Lᵢ(P) / Lⱼ(P)

所以

    k < 0  ⟺  Lᵢ(P) / Lⱼ(P) > 0  ⟺  Lᵢ(P) 與 Lⱼ(P) **同號**

（題目保證重要地點不在任何道路上，所以 Lᵢ(P) 不會是 0，不用煩惱除以零。）

於是問題化成：對每個重要地點 P，把 N 條直線分成「代入後為正」與「代入後為負」兩堆，
設個數是 s 與 t，則這個地點貢獻

    C(s, 2) + C(t, 2)

把所有地點加起來就是答案。完全不用求任何交點！

複雜度 O(N · P)，3000 × 100 = 30 萬次，瞬間完成（原本的 J × P ≈ 450 萬 × 100 就爆了）。

【驗算】我把兩組範例都算過：
    第一組 5 條路、3 個地點 → 16 ✓
    第二組 6 條路、3 個地點 → 23 ✓
兩組都跟題目輸出一致。

【幾何直覺】Lᵢ(P) 的正負就是「P 落在第 i 條路的哪一側」。兩條路同號代表 P 落在它們交點所張的
「同一組對頂角」裡，這時連線的 k 就是負的。`,
    t: `1. 千萬不要真的去枚舉路口——N = 3000 有將近 450 萬個交點，乘上 100 個地點直接爆掉。只要對每個地點統計 N 條直線代入後的正負號即可。
2. 判斷條件是「Lᵢ(P) 與 Lⱼ(P) 同號」，不是異號。從 k = −Lᵢ/Lⱼ 推一次就不會記反。
3. 答案是 C(s,2) + C(t,2) 對所有地點求和，最大約 100 × C(3000,2) ≈ 4.5 × 10^8，超過 int 上限，一定要用 long long。
4. 代入值 a·x + b·y + c 的量級是 10000 × 座標，用 long long 比較保險。
5. 題目保證地點不在道路上，所以不會出現 Lᵢ(P) = 0；但實作上仍建議把 ≥ 0 / < 0 分乾淨。
6. 輸出是兩行：「Roadmap n:」與「Negative Builders Ltd. will build X New Roads.」，注意 Ltd 後面有句點、New Roads 兩個字都大寫。
7. 結束條件是讀到 N = 0。`,
    c: `#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int n, cs = 0;
    while (cin >> n && n != 0) {
        vector<long long> A(n), B(n), C(n);
        for (int i = 0; i < n; i++) cin >> A[i] >> B[i] >> C[i];
        int p;
        cin >> p;
        long long tot = 0;
        for (int q = 0; q < p; q++) {
            long long x, y;
            cin >> x >> y;
            long long s = 0, t = 0;                 // 代入後為正 / 為負的直線數
            for (int i = 0; i < n; i++) {
                long long v = A[i] * x + B[i] * y + C[i];
                if (v > 0) s++; else t++;
            }
            tot += s * (s - 1) / 2 + t * (t - 1) / 2;   // 同號的配對數
        }
        cout << "Roadmap " << ++cs << ":\n";
        cout << "Negative Builders Ltd. will build " << tot << " New Roads.\n";
    }
    return 0;
}`
  }
};
