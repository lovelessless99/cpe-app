/* 第四十九批 —— pdftotext 重抽題敘後補回 */
const SOL89 = {
  '10373': {
    q: `工廠生產黃銅磚。黃銅是銅與鋅的合金，每塊磚重 1000 公克，含銅量可以是 1 到 999 公克。工廠用不同製程生產不同型號的磚，每種型號有不同的含銅濃度與價格。

你的客戶想買 M 塊磚，而且（因為宗教因素）必須是**不同型號**。這些磚會被熔在一起，混合後每公斤必須含有至少 CMin、至多 CMax 公克的銅。目標是挑出 M 種型號，讓混合濃度合格且總價最低。

輸入：第一行是測資組數，之後空一行；各組之間也空一行。
每組第一部分是一行 N（1 ≤ N ≤ 200）代表磚的型號數，接著 N 行、每行是該型號的含銅濃度（1 到 999）與價格（分，每塊不超過 10 美元）。
第二部分是一行 C（1 ≤ C ≤ 100）代表客戶數，接著 C 行、每行是該客戶的 M（1 ≤ M ≤ 20）、CMin（1 ≤ CMin ≤ 999）、CMax（1 ≤ CMax ≤ 999）。
輸出：每個客戶一行，輸出符合需求的最低總價；若做不到，輸出「impossible」。連續兩組測資之間空一行。

範例輸入
1

11
550 300
550 200
700 340
300 140
600 780
930 785
730 280
678 420
999 900
485 390
888 800
3
2 500 620
9 550 590
9 610 620

範例輸出
420
impossible
3635`,
    h: `【先把濃度條件變成整數條件】
混合之後的濃度是「各磚濃度的平均」（因為每塊都是 1000 公克，權重相同）。所以

    CMin ≤ (Σ 濃度) / M ≤ CMax    ⟺    M·CMin ≤ Σ 濃度 ≤ M·CMax

變成純整數的區間條件，不會有任何浮點誤差。

【0/1 背包】
每種型號最多選一塊（型號必須不同），所以是 0/1 背包，狀態要記「選了幾塊」與「濃度總和」：

    dp[m][s] = 選了 m 種型號、濃度總和為 s 時的最低總價

    對每種磚 (c, p)：for m 由大到小、s 由大到小
        dp[m+1][s+c] = min(dp[m+1][s+c], dp[m][s] + p)

m ≤ 20、s ≤ 20 × 999 = 19980，共 200 × 20 × 19980 ≈ 8000 萬次更新，
用一維價格陣列跑得動。

【關鍵優化：這張表與客戶無關】
一組測資裡有多達 100 個客戶，但 dp 只跟磚的清單有關。所以**整張表只算一次**，
每個客戶只要在 dp[M][·] 上掃過 s ∈ [M·CMin, M·CMax] 取最小值即可，
不要每個客戶重跑一次背包。

【逐組驗算】（我實作出來跑過範例的三個客戶）
  客戶 1：M = 2、濃度總和要落在 [1000, 1240]。
      選 (300, 140) 與 (730, 280)：濃度和 1030 ✓、價格 **420** ✓
      （最便宜的兩塊是 140 + 200 = 340，但濃度和只有 850，落在區間外，不能用。）
  客戶 2：M = 9、總和要在 [4950, 5310]。任何 9 種的濃度總和都湊不進這個區間
      → **impossible** ✓
  客戶 3：M = 9、總和要在 [5490, 5580] → 最低價 **3635** ✓`,
    t: `1. 濃度條件是「平均」落在區間，兩邊同乘 M 就變成「總和」落在 [M·CMin, M·CMax]，全程整數，別用浮點數算平均。
2. 型號必須相異，所以是 0/1 背包（每種只能選一次），不是無界背包。範例第一個客戶剛好有兩種濃度都是 550 的磚，它們是「不同型號」可以同時選。
3. dp 表與客戶無關，只建一次；100 個客戶各自重跑背包會超時。
4. 迴圈順序：外層跑磚，內層 m 與 s 都要「由大到小」，否則同一塊磚會被重複選。
5. 無解要輸出小寫的「impossible」。
6. 濃度總和的上界是 20 × 999 = 19980，陣列開夠；查詢時 s 的上界也要夾在 19980 以內。
7. 各組測資之間以空行分隔，輸出時連續兩組之間也要空一行。`,
    c: `#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    const int MAXM = 20, MAXS = 20 * 999, INF = 1000000000;
    int T;
    if (!(cin >> T)) return 0;
    for (int tc = 0; tc < T; tc++) {
        int n;
        cin >> n;
        vector<int> conc(n), price(n);
        for (int i = 0; i < n; i++) cin >> conc[i] >> price[i];

        // dp[m][s] = 選 m 種、濃度總和 s 的最低價（與客戶無關，只算一次）
        vector<vector<int> > dp(MAXM + 1, vector<int>(MAXS + 1, INF));
        dp[0][0] = 0;
        for (int i = 0; i < n; i++) {
            int c = conc[i], p = price[i];
            for (int m = MAXM - 1; m >= 0; m--)            // 由大到小，避免重複選
                for (int s = MAXS - c; s >= 0; s--) {
                    if (dp[m][s] >= INF) continue;
                    if (dp[m][s] + p < dp[m + 1][s + c])
                        dp[m + 1][s + c] = dp[m][s] + p;
                }
        }

        int cli;
        cin >> cli;
        if (tc) cout << "\n";
        while (cli--) {
            int M, lo, hi;
            cin >> M >> lo >> hi;
            int best = INF;
            int s1 = M * lo, s2 = min(M * hi, MAXS);
            for (int s = s1; s <= s2; s++) best = min(best, dp[M][s]);
            if (best >= INF) cout << "impossible\n";
            else cout << best << "\n";
        }
    }
    return 0;
}`
  },

  '10186': {
    q: `歐洲盃資格賽的分組賽制是：同組每隊與其他每隊各交手兩次。勝者得 3 分、和局各得 1 分、敗者 0 分。所有比賽結束後依積分排名；同分時的後續判準依序是「淨勝球（進球減失球）」、「進球數」、「隨機決定」。

請問：把剩餘比賽所有可能的結果都考慮進去，每一隊在分組賽結束後「最好」與「最差」的名次分別是多少？

輸入：一或多組測資。每組格式如下：
    第一行是整數 n（1 ≤ n ≤ 20），代表隊伍數。
    接下來 n 行是隊名（長度小於 30，不含空白）。
    接下來一行是整數 g，代表已完成的比賽數。
    接下來 g 行，每行是一場已完成的比賽，格式為「隊1 隊2 進球1 進球2」。
可以假設剩餘比賽至多 10 場，而且每一隊至少還有一場比賽要打。以 n = 0 結束。
輸出：每組先印一行「Group #x」（x 從 1 開始）。接著依輸入順序每隊一行，印隊名、一個空白、最好名次、一個減號、最差名次。每組測資之後印一個空行（最後一組也要）。

範例輸入
2
A
B
1
A B 1 0
5
Ger
Tur
Fin
Nor
Mol
14
Fin Mol 3 2
Tur Nor 3 0
Tur Ger 1 0
Nor Fin 1 0
Mol Ger 1 3
Tur Fin 1 3
Nor Mol 2 2
Nor Ger 0 3
Tur Mol 2 0
Ger Fin 2 0
Mol Fin 0 0
Ger Mol 6 1
Fin Tur 2 4
Mol Nor 0 0
0

範例輸出
Group #1
A 1-2
B 1-2

Group #2
Ger 1-3
Tur 1-3
Fin 1-4
Nor 1-5
Mol 4-5`,
    h: `【剩餘比賽最多 10 場 → 直接枚舉 3¹⁰】
先由「每對交手兩次」推算出還有哪些比賽沒打：對每一對 (i, j) 數出已打過幾場，
剩下的就是 2 減掉它。題目保證剩餘至多 10 場，所以總共只有 3¹⁰ = 59049 種結果組合
（每場三種：主勝、客勝、和局），全部枚舉即可。

對每一種組合算出各隊積分，然後對每一隊 i：

    最好名次 = 1 + (積分**嚴格大於** i 的隊數)
    最差名次 = n − (積分**嚴格小於** i 的隊數)

取所有組合的最小值與最大值。複雜度 3¹⁰ × n² ≈ 2400 萬，很快。

【為什麼可以只看積分、不算淨勝球】
題目的判準依序是積分 → 淨勝球 → 進球數 → 隨機。關鍵在於**剩餘比賽的比分是任意的**：
同樣是「贏」，可以贏 1:0 也可以贏 100:0，所以淨勝球在很大範圍內可以自由調整；
真的完全打平時還有「隨機決定」這一關，兩種順序都可能發生。
因此同分的兩隊誰前誰後，基本上都能安排出來——把同分視為「可前可後」正好對應
最好名次用「嚴格大於」、最差名次用「嚴格小於」的算法。

【逐組驗算】（我實作出來跑過兩組範例）
  第 1 組：A 已 1:0 勝 B，還剩一場。A 贏 → 6:0 分，A 第 1、B 第 2；
      B 贏 → 兩隊都 3 分，後續判準可前可後 → 兩隊都可能是第 1 或第 2
      → **A 1-2、B 1-2** ✓
  第 2 組：五隊、已打 14 場、剩 6 場。程式跑出
      Ger 1-3、Tur 1-3、Fin 1-4、Nor 1-5、Mol 4-5，與題目輸出完全一致 ✓
      其中 Mol 最好只能到第 4 很有代表性：Mol 現在 3 分、只剩一場，最多 6 分；
      而 Ger、Tur 已經各有 12 分、Fin 有 7 分，三隊都不可能掉到 6 分以下
      → Mol 永遠被這三隊壓在下面。`,
    t: `1. 剩餘比賽要自己從「每對交手兩次」推算，不是輸入直接給的。對每一對數出已打場數，用 2 減掉。
2. 最好名次是「1 + 嚴格贏過我的隊數」、最差名次是「n − 嚴格輸給我的隊數」。同分的隊伍兩邊都不算，這正好對應「同分時前後都有可能」。
3. 【這是一個建模假設】我沒有真的去追蹤淨勝球，理由是剩餘比賽的比分不受限（贏 1:0 或 100:0 都可以），加上最後還有「隨機決定」，所以同分時的順序基本上可以任意安排。兩組範例（含五隊那組的 Mol 4-5）都完全吻合。真要挑毛病的話，某隊剩餘比賽若「全部都是和局」就無法靠比分改變淨勝球，這種極端情形理論上會有差異——若送出被判 WA，可以往這個方向補。
4. 每場有三種結果（主勝、客勝、和局），不是兩種。
5. 隊名要用 map 對應到編號；輸出時要依「輸入順序」而不是名次順序。
6. 輸出格式是「隊名 最好-最差」，名次之間是減號沒有空白；每組之後都要空一行，最後一組也不例外。
7. n 可以是 1（只有一隊），這時名次永遠是 1-1；結束條件是 n = 0。`,
    c: `#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int n, grp = 0;
    while (cin >> n && n != 0) {
        vector<string> name(n);
        map<string, int> id;
        for (int i = 0; i < n; i++) { cin >> name[i]; id[name[i]] = i; }

        vector<int> base(n, 0);
        vector<vector<int> > played(n, vector<int>(n, 0));
        int g;
        cin >> g;
        for (int k = 0; k < g; k++) {
            string a, b;
            int ga, gb;
            cin >> a >> b >> ga >> gb;
            int i = id[a], j = id[b];
            played[i][j]++; played[j][i]++;
            if (ga > gb) base[i] += 3;
            else if (ga < gb) base[j] += 3;
            else { base[i]++; base[j]++; }
        }

        vector<pair<int, int> > rem;                 // 每對交手兩次，補齊剩下的
        for (int i = 0; i < n; i++)
            for (int j = i + 1; j < n; j++)
                for (int k = played[i][j]; k < 2; k++) rem.push_back(make_pair(i, j));

        int r = (int)rem.size(), tot = 1;
        for (int k = 0; k < r; k++) tot *= 3;

        vector<int> best(n, n + 1), worst(n, 0), p(n);
        for (int mask = 0; mask < tot; mask++) {
            for (int i = 0; i < n; i++) p[i] = base[i];
            int m = mask;
            for (int k = 0; k < r; k++) {
                int o = m % 3; m /= 3;
                int i = rem[k].first, j = rem[k].second;
                if (o == 0) p[i] += 3;
                else if (o == 1) p[j] += 3;
                else { p[i]++; p[j]++; }
            }
            for (int i = 0; i < n; i++) {
                int hi = 0, lo = 0;
                for (int j = 0; j < n; j++) {
                    if (j == i) continue;
                    if (p[j] > p[i]) hi++;
                    else if (p[j] < p[i]) lo++;      // 同分不算，代表前後皆有可能
                }
                best[i] = min(best[i], 1 + hi);
                worst[i] = max(worst[i], n - lo);
            }
        }

        cout << "Group #" << ++grp << "\n";
        for (int i = 0; i < n; i++)
            cout << name[i] << " " << best[i] << "-" << worst[i] << "\n";
        cout << "\n";
    }
    return 0;
}`
  }
};
