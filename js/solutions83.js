/* 第四十三批 —— pdftotext 重抽題敘後補回 */
const SOL83 = {
  '10280': {
    q: `酒瓶永遠不會裝滿：瓶頸要留一點空氣讓酒熱脹冷縮。留太少，酒膨脹會把軟木塞頂出來；留太多，酒會壞掉。所以每種瓶子都有「最小容量」與「最大容量」。

給你一定量的酒與若干種尺寸的瓶子，決定要用哪些瓶子，使得每個瓶子都裝到介於它的最小與最大容量之間，而且裝進瓶子的酒越多越好。

輸入：第一行是測資組數，之後空一行；每組之間也空一行。
每組第一行是兩個整數：要裝瓶的酒量（公升，0 到 1,000,000）與瓶子的種類數（1 到 100）。接下來每種瓶子一行，給出它的最小與最大容量（毫升）。最大容量不小於 325 且不超過 4500；最小容量不小於最大容量的 95%、也不大於 99%。每種瓶子的數量無限。
輸出：每組一個整數——「無法裝瓶的酒量」（毫升）。連續兩組之間空一行。

範例輸入
2

10 2
4450 4500
725 750

10000 2
4450 4500
725 750

範例輸出
250

0`,
    h: `【先把問題整理乾淨】
若第 i 種瓶子用了 k_i 個，那麼這組瓶子能裝的總量剛好是一個連續區間

    A = Σ k_i·min_i   到   B = Σ k_i·max_i

（因為每個瓶子在自己的區間內可以任意調整，整數量下這些區間的和仍是連續區間。）
所以只要 A ≤ W，就能裝進 min(W, B) 的酒。於是問題變成一個無界背包：

    重量 = min_i、價值 = max_i、容量 = W，最大化總價值 B

答案 = W − min(W, B_max)。

【難點：W 可以到 10^9 毫升，DP 做不動】
關鍵是證明「W 只要夠大，答案一定是 0」，而且這個門檻其實非常小。

設最佳解的 B < W（也就是答案不是 0）。那麼對每一種瓶子 i 都必須有

    A + min_i > W        （否則再加一個瓶子就能提高 B 而且沒有超過 W）

於是 W − A < min_i ≤ 4500，因此
    B − A ≤ W − A < 4500  ⟹  Σ k_i·(max_i − min_i) < 4500

而題目保證 min_i ≤ 0.99·max_i，也就是 max_i − min_i ≥ 0.01·max_i，換句話說

    max_i ≤ 100·(max_i − min_i)

所以
    A < Σ k_i·max_i ≤ 100·Σ k_i·(max_i − min_i) < 100 × 4500 = 450000

再加上 W < A + 4500，得到

    **W < 454500 毫升（約 454.5 公升）**

也就是說：只要酒量 ≥ 454500 毫升，答案必定是 0；否則 DP 的容量最多 454499，
乘上 100 種瓶子只有約 4500 萬次運算，非常快。

【DP】dp[a] = 「min 總和不超過 a 毫升」時能達到的最大 B。
    dp[0] = 0
    dp[a] = max( dp[a−1], max over i of dp[a − min_i] + max_i )
答案 = W − min(W, dp[W])。

【逐組驗算】（我實作出來跑過兩組範例）
  第 1 組：10 公升 = 10000 毫升。
      最佳是 2 個大瓶 + 1 個小瓶：A = 4450×2 + 725 = 9625 ≤ 10000，B = 4500×2 + 750 = 9750。
      再加一個小瓶的話 A 會變成 10350 > 10000，不行。
      → 裝了 9750，剩下 **250** ✓
  第 2 組：10000 公升 = 10^7 毫升 ≥ 454500 → 直接 **0** ✓`,
    t: `1. 輸入的酒量單位是「公升」，瓶子容量是「毫升」，一定要先乘 1000。
2. 直接對 W 開 DP 會爆（10^9）。要先證出「W ≥ 454500 毫升時答案必為 0」這個門檻，DP 才做得動。這個門檻來自 min ≥ 0.95·max 這個看似無關的條件。
3. 每個瓶子能裝的是一個區間而不是固定值，所以 k 個瓶子的總量是連續區間 [Σmin, Σmax]，不要當成只能裝 max。
4. 背包的「重量」是 min、「價值」是 max，兩者不要弄反。
5. 酒量可能是 0，這時答案就是 0，要能正確處理（DP 陣列大小記得 +1）。
6. 輸出只有一個整數（毫升）；連續兩組之間要空一行，最後一組後面不要多空行。
7. 瓶子數量無限，所以是「無界背包」，內層迴圈由小到大掃。`,
    c: `#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int T;
    if (!(cin >> T)) return 0;
    for (int tc = 0; tc < T; tc++) {
        long long litres;
        int k;
        cin >> litres >> k;
        vector<int> mn(k), mx(k);
        for (int i = 0; i < k; i++) cin >> mn[i] >> mx[i];

        long long W = litres * 1000;                 // 公升 -> 毫升
        long long left;
        if (W >= 454500) left = 0;                   // 超過門檻一定裝得完
        else {
            int w = (int)W;
            vector<int> dp(w + 1, 0);                // dp[a] = min 總和 <= a 時的最大 B
            for (int a = 1; a <= w; a++) {
                dp[a] = dp[a - 1];
                for (int i = 0; i < k; i++)
                    if (mn[i] <= a && dp[a - mn[i]] + mx[i] > dp[a])
                        dp[a] = dp[a - mn[i]] + mx[i];
            }
            left = W - min<long long>(W, dp[w]);
        }

        if (tc) cout << "\n";
        cout << left << "\n";
    }
    return 0;
}`
  },

  '10329': {
    q: `組合數 nCr 代表「從 n 個東西中取出 r 個的方法數」，定義為 n! / ((n−r)!·r!)。

一個組合表達式分成分子與分母兩部分，兩部分都是「若干個 nCr 相乘」或「單一個 nCr」。例如 nCr × nCr / nCr。

請判斷這個表達式是否得到整數結果（分子是否被分母整除）。若是，且結果的位數小於 101，就印出結果；位數 ≥ 101 則印「-1」。若不能整除，只印 0。

輸入：每組先是兩個正整數 N 與 M（N, M ≤ 100）。接下來 N + M 行，每行兩個整數 n 與 r；前 N 行組成分子、後 M 行組成分母。保證不會出現非法的 nCr（0 ≤ r ≤ n ≤ 5000）。讀到 EOF 結束。
輸出：每組一行，依上述規則輸出。

範例輸入
3 3
10 5
10 4
10 3
10 7
10 6
10 5
3 3
10 5
10 4
10 3
10 7
10 6
10 1
3 3
10 5
10 4
10 3
10 7
10 6
10 10
4 1
10 5
10 5
10 5
10 5
100 100

範例輸出
1
0
252
4032758016`,
    h: `數字大到爆（C(5000,2500) 就有一千多位，還要連乘 100 個），所以千萬不要真的去算再相除。
正確做法是**全程只操作質因數的指數**。

【第一步：指數向量】
質數 p 在 n! 中的指數是 Legendre 公式

    e_p(n!) = ⌊n/p⌋ + ⌊n/p²⌋ + ⌊n/p³⌋ + …

因此
    e_p(C(n,r)) = e_p(n!) − e_p(r!) − e_p((n−r)!)

把分子的每一項加進一個指數向量、分母的每一項減掉。n ≤ 5000，所以只要 5000 以內的
669 個質數就夠了。

【第二步：三種答案】
1. 若有任何一個質數的指數是**負的** → 分子不被分母整除 → 印 **0**
2. 否則結果 = Π p^e。先用對數估位數：

       位數 ≈ ⌊Σ e_p · log₁₀(p)⌋ + 1

   估出來明顯超過 100 就直接印 **-1**，連算都不用算。
3. 位數在範圍內時才用大數把 Π p^e 乘出來印。因為保證不超過 100 位，
   總共只需要約 330 次「大數乘小數」，非常輕鬆。

（估位數時留一點餘裕比較保險：估計值 ≤ 110 才進去算，算完再用實際長度判斷是否 > 100。
浮點誤差就不會造成誤判。）

【逐組驗算】（我實作出來跑過四組範例）
  第 1 組：分子 252 × 210 × 120 = 6350400，分母 120 × 210 × 252 = 6350400 → **1** ✓
  第 2 組：分母換成 120 × 210 × 10 = 252000，6350400 / 252000 = 25.2 不是整數 → **0** ✓
      （用指數向量看，就是某個質數的指數變成負的。）
  第 3 組：分母 120 × 210 × 1 = 25200 → 6350400 / 25200 = **252** ✓
  第 4 組：分子 252⁴ = 4032758016，分母 C(100,100) = 1 → **4032758016** ✓
      這組也順便提醒：結果可能超過 32 位元整數，一定要用大數（或至少 long long）。`,
    t: `1. 絕對不要直接算 nCr 再相乘相除——C(5000,2500) 有一千多位。全程只加減質因數指數。
2. 判斷整除的方法是「有沒有任何質數的指數為負」，不是去試除。
3. 位數估計用 Σ e_p·log₁₀(p) + 1；浮點誤差可能差一位，所以估計值要留餘裕（例如 ≤ 110 才真的去算），最後用大數的實際長度做最終判斷。
4. 三種輸出要分清楚：不能整除印 0、能整除但位數 ≥ 101 印 -1、其餘印結果本身。
5. C(n,0) = C(n,n) = 1，指數全是 0，不會影響結果；r = 0 或 r = n 都是合法輸入。
6. 結果可能超過 long long（第四組是 40 億，還在範圍內，但一般情況會更大），要用大數。
7. 讀到 EOF 結束，用 while (cin >> N >> M) 判斷。`,
    c: `#include <bits/stdc++.h>
using namespace std;

const int LIM = 5000;
vector<int> primes;

int legendre(int n, int p) {                 // p 在 n! 中的指數
    int s = 0;
    for (long long q = p; q <= n; q *= p) s += (int)(n / q);
    return s;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    vector<char> comp(LIM + 1, 0);
    for (int i = 2; i <= LIM; i++) {
        if (comp[i]) continue;
        primes.push_back(i);
        for (long long j = (long long)i * i; j <= LIM; j += i) comp[j] = 1;
    }

    int N, M;
    while (cin >> N >> M) {
        vector<int> e(primes.size(), 0);
        for (int t = 0; t < N + M; t++) {
            int n, r, sg = (t < N) ? 1 : -1;
            cin >> n >> r;
            for (size_t i = 0; i < primes.size(); i++) {
                int p = primes[i];
                if (p > n) break;
                e[i] += sg * (legendre(n, p) - legendre(r, p) - legendre(n - r, p));
            }
        }

        bool divisible = true;
        for (size_t i = 0; i < e.size(); i++) if (e[i] < 0) divisible = false;
        if (!divisible) { cout << "0\n"; continue; }

        double dig = 0;                                    // 先用對數估位數
        for (size_t i = 0; i < e.size(); i++) dig += e[i] * log10((double)primes[i]);
        if (dig > 110) { cout << "-1\n"; continue; }

        vector<int> big;                                   // 十進位，低位在前
        big.push_back(1);
        for (size_t i = 0; i < e.size(); i++)
            for (int k = 0; k < e[i]; k++) {
                int carry = 0;
                for (size_t j = 0; j < big.size(); j++) {
                    int v = big[j] * primes[i] + carry;
                    big[j] = v % 10;
                    carry = v / 10;
                }
                while (carry) { big.push_back(carry % 10); carry /= 10; }
            }
        if ((int)big.size() > 100) { cout << "-1\n"; continue; }
        for (int j = (int)big.size() - 1; j >= 0; j--) cout << big[j];
        cout << "\n";
    }
    return 0;
}`
  }
};
