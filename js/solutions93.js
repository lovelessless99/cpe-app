/* 第五十三批 —— pdftotext 重抽題敘後補回 */
const SOL93 = {
  '10216': {
    q: `三所大學的校區可以視為平面上的三個點 A、B、C。它們要蓋一間共用的咖啡店，地點選在「到三個校區的距離總和最小」的位置 P，請求出這個最小總距離（也就是 PA + PB + PC）。

另外三所大學各自提出一個方案：三角形 ABC 的內心 I、重心 G、外心 O。所以你也要印出 (AI + BI + CI)、(AG + BG + CG)、(AO + BO + CO)。若某個值無法決定，就把它印成 −1（依同樣格式）。

輸入：第一行是整數 S（0 < S ≤ 100）代表測資組數。接下來 S 行，每行三個整數 a、b、c，a 是 B 與 C 的距離、b 是 C 與 A 的距離、c 是 A 與 B 的距離（0 < a, b, c ≤ 10000）。
輸出：每組輸出四個浮點數 DM、DI、DG、DO，各取小數點後三位。

範例輸入
2
3 3 3
4 4 4

範例輸出
5.196 5.196 5.196 5.196
6.928 6.928 6.928 6.928`,
    h: `四個值都有封閉公式，只要小心「費馬點」的分支。

設 s = (a+b+c)/2、K = 三角形面積（海龍公式 √(s(s−a)(s−b)(s−c))）。

【DM：費馬點（Fermat point）】
    若三個內角都小於 120°：
        DM = √( (a² + b² + c²)/2 + 2√3·K )
    若某個內角 ≥ 120°：最佳點就是**那個頂點本身**，總和是它相鄰兩邊之和。
        例如角 A ≥ 120° 時 DM = b + c。
    判斷用餘弦定理：cos A = (b² + c² − a²)/(2bc) ≤ −1/2 就代表角 A ≥ 120°。

【DI：內心】用重心座標，內心 I = (a·A + b·B + c·C)/(a+b+c)，算出座標後直接量三段距離。
（也可以用 AI = r / sin(A/2)，r = K/s。）

【DG：重心】重心到頂點的距離是中線的 2/3，而中線 m_a = ½√(2b² + 2c² − a²)，所以
    GA = (1/3)·√(2b² + 2c² − a²)，三個加起來就是 DG。

【DO：外心】外心到三個頂點的距離都是外接圓半徑 R = abc/(4K)，所以 **DO = 3R**，
不用真的求外心座標。

【實作建議】把 B 放在原點、C 放在 (a, 0)、A 用餘弦定理算出座標，其餘用座標直接算最省事。

【驗算】我把公式與「數值最小化 PA+PB+PC」的爬山法逐一比對過七組：
    (3,3,3) → 5.196、(4,4,4) → 6.928（與題目範例一致 ✓）
    (3,4,5) → 6.766、(7,8,9) → 13.782、(2,3,4) → 4.956
    **(10,3,8) → 11.000**：這組的最大角超過 120°，公式走的是「b + c = 3 + 8」這條分支，
        數值最小化也得到 11.000，正好驗證了分支判斷 ✓
    (5,5,9.9) → 10.000（極扁的三角形，DO 高達 53.166，因為外接圓半徑爆大）
四個值在所有組別都與座標法算出來的結果吻合。`,
    t: `1. 費馬點有兩種情形：三角都小於 120° 用根號公式，否則最佳點就是那個鈍角頂點、答案是兩鄰邊之和。只寫根號公式在 (10,3,8) 這種輸入就會錯。
2. 判斷 120° 用 cos ≤ −1/2，不要用角度比較（會多一次三角函數的誤差）。
3. DO 就是 3R = 3abc/(4K)，不必求外心座標；但三角形很扁時 R 會非常大（(5,5,9.9) 的 DO 是 53.166），別誤以為算錯。
4. 重心到頂點是「中線的 2/3」不是 1/2。
5. 四個值都取小數點後三位，同一行以空白分隔。
6. a、b、c 是「對邊」：a 對 A、b 對 B、c 對 C。代公式時對應關係弄反會全錯。
7.【這題我標了不確定】原文說「若某個值無法決定就印成 −1」，但 PDF 抽出來時掉了負號（顯示成 1）。我照常理判斷是 −1，格式同樣是三位小數（−1.000）。另外「無法決定」指的應該是 a、b、c 構不成三角形（違反三角不等式）的情形，此時我把四個值全印成 −1.000；但也有可能題目只把外心那一項當成無法決定（退化三角形的外接圓半徑發散）。範例全是正三角形，測不到這種情形。`,
    unsure: true,
    c: `#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout << fixed << setprecision(3);
    int S;
    if (!(cin >> S)) return 0;
    while (S--) {
        double a, b, c;
        cin >> a >> b >> c;
        if (a + b <= c || b + c <= a || a + c <= b) {       // 構不成三角形
            cout << -1.0 << " " << -1.0 << " " << -1.0 << " " << -1.0 << "\n";
            continue;
        }
        double s = (a + b + c) / 2.0;
        double K = sqrt(s * (s - a) * (s - b) * (s - c));

        // 把 B 放原點、C 放 (a,0)，用餘弦定理定出 A
        double ax = (a * a + c * c - b * b) / (2 * a);
        double ay = sqrt(max(0.0, c * c - ax * ax));
        double Ax = ax, Ay = ay, Bx = 0, By = 0, Cx = a, Cy = 0;

        double cosA = (b * b + c * c - a * a) / (2 * b * c);
        double cosB = (a * a + c * c - b * b) / (2 * a * c);
        double cosC = (a * a + b * b - c * c) / (2 * a * b);
        double DM;
        if (cosA <= -0.5) DM = b + c;                        // 角 A >= 120 度
        else if (cosB <= -0.5) DM = a + c;
        else if (cosC <= -0.5) DM = a + b;
        else DM = sqrt((a * a + b * b + c * c) / 2.0 + 2.0 * sqrt(3.0) * K);

        double p = a + b + c;
        double Ix = (a * Ax + b * Bx + c * Cx) / p;          // 內心的重心座標
        double Iy = (a * Ay + b * By + c * Cy) / p;
        double Gx = (Ax + Bx + Cx) / 3.0, Gy = (Ay + By + Cy) / 3.0;

        double DI = hypot(Ax - Ix, Ay - Iy) + hypot(Bx - Ix, By - Iy) + hypot(Cx - Ix, Cy - Iy);
        double DG = hypot(Ax - Gx, Ay - Gy) + hypot(Bx - Gx, By - Gy) + hypot(Cx - Gx, Cy - Gy);
        double DO = 3.0 * (a * b * c / (4.0 * K));           // 外心到三頂點都是 R

        cout << DM << " " << DI << " " << DG << " " << DO << "\n";
    }
    return 0;
}`
  },

  '10335': {
    q: `有一個 n 個頂點的凸多邊形，其中一條邊上有一個發光點，往某個已知方向射出一道光。多邊形的每條邊都是鏡子，所以光碰到邊時會依反射定律改變方向。經過若干次反射之後，請算出光線打到的點。另外，若光線打到多邊形的**頂點**，它就永遠消失了。

輸入：每組先是兩個數 3 ≤ v ≤ 10 與 0 ≤ n ≤ 1000，分別是多邊形的頂點數與要模擬的反射次數。接著一行是 (x, y) 發光點的位置（一定在某條邊上，且不會與任何頂點重合）與 0 ≤ t < 360，光線與 x 軸的夾角（度，逆時針量，保證合法）。接下來 v 行是多邊形的頂點，依逆時針排列。所有點的座標都取到小數點後兩位。以兩個 0 結束。
輸出：每組輸出光線在 n 次反射之後打到的點，取小數點後兩位（該點一定落在多邊形的某條邊上）。若在 n 次反射之內光線就消失了，輸出「lost forever...」。每組輸出各自一行。

請特別小心精度誤差：若有一個頂點是 4.00 而光線通過 4.001，就要算成「lost forever...」，因為我們只在意小數點後兩位。

範例輸入
4 4
2.00 0.00 0.00
0.00 0.00
4.00 0.00
4.00 4.00
0.00 4.00
4 0
2.00 0.00 45.00
0.00 0.00
4.00 0.00
4.00 4.00
0.00 4.00
0 0

範例輸出
lost forever...
4.00 2.00`,
    h: `【先弄清楚 n 的語意】
n 是「反射次數」。n = 0 代表不反射，直接輸出光線第一次打到邊界的位置——
範例第二組就是這樣：從 (2,0) 以 45° 射出，第一次打到 (4.00, 2.00) ✓。
所以要輸出的是「第 n+1 個打到的點」。

【每一步做三件事】
1. **求最近的交點**：把光線寫成 P + t·d，對每條邊 (V[i], V[i+1]) 解交點，
   取 t > 0 之中最小的那個。因為是凸多邊形，這個交點唯一。
   （t 要用一個小的下界如 1e−9，避免又打到自己出發的那條邊。）
2. **檢查是不是打到頂點**：交點與任何一個頂點的距離小於容忍值就是「lost forever...」。
3. **反射**：設該邊的單位法向量是 n̂，則新方向 d' = d − 2(d·n̂)n̂。

【驗算】兩組範例都對：
  第一組從 (2,0) 沿 +x 方向直接撞上頂點 (4,0) → **lost forever...** ✓
  第二組 n = 0 → **4.00 2.00** ✓
另外我自己推了一個檢查（**這是我造的例子，不是題目的**）：同樣是 4×4 正方形、
從 (2,0) 以 45° 射出，路徑會形成週期 4 的循環
    (4,2) → (2,4) → (0,2) → (2,0) → (4,2) → …
手推的結果與程式輸出完全一致，可以用來驗證反射向量寫對了沒。

【複雜度】每次反射掃過 v ≤ 10 條邊，n ≤ 1000，一組最多一萬次運算，完全不是問題。`,
    t: `1. n = 0 時**不反射**，直接輸出第一次打到的點。輸出的是「第 n+1 個打到的點」，很容易差一。
2. 求交點時 t 要有一個嚴格正的下界，否則會立刻又「打到」自己出發的那條邊而卡住。
3. 反射公式是 d − 2(d·n̂)n̂，n̂ 必須是**單位**法向量，忘了正規化會讓方向長度爆掉。
4. 角度是度，要先乘 π/180；而且 t 的定義是「與 x 軸的夾角、逆時針量」。
5. 「打到頂點就消失」要在**每一次**打到邊界時都檢查，不是只檢查最後一次。
6. 輸出兩位小數；「lost forever...」結尾是三個點。
7.【這題我標了不確定】題目說「若有一個頂點是 4.00 而光線通過 4.001 就算 lost」，代表判定頂點的容忍值至少要 0.001，但沒有明確給出上界。我用 1e−3（含等號）；若容忍值該取得更寬（例如 5e−3，因為座標只有兩位小數），結果會不一樣。範例的第一組是「正正好」打到頂點，測不出這個界線。`,
    unsure: true,
    c: `#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout << fixed << setprecision(2);
    const double EPS = 1e-3;                    // 判定「打到頂點」的容忍值
    int v, n;
    while (cin >> v >> n) {
        if (v == 0 && n == 0) break;
        double px, py, ang;
        cin >> px >> py >> ang;
        vector<double> VX(v), VY(v);
        for (int i = 0; i < v; i++) cin >> VX[i] >> VY[i];

        double dx = cos(ang * acos(-1.0) / 180.0);
        double dy = sin(ang * acos(-1.0) / 180.0);
        bool lost = false;
        double hx = 0, hy = 0;

        for (int step = 0; step <= n; step++) {
            double bt = 1e18;
            int bi = -1;
            for (int i = 0; i < v; i++) {
                double x1 = VX[i], y1 = VY[i];
                double x2 = VX[(i + 1) % v], y2 = VY[(i + 1) % v];
                double ex = x2 - x1, ey = y2 - y1;
                double den = dx * ey - dy * ex;
                if (fabs(den) < 1e-12) continue;
                double t = ((x1 - px) * ey - (y1 - py) * ex) / den;
                double u = ((x1 - px) * dy - (y1 - py) * dx) / den;
                if (t > 1e-9 && u > -1e-9 && u < 1 + 1e-9 && t < bt) { bt = t; bi = i; }
            }
            if (bi < 0) { lost = true; break; }
            hx = px + bt * dx;
            hy = py + bt * dy;

            for (int i = 0; i < v; i++)                     // 打到頂點就消失
                if (hypot(hx - VX[i], hy - VY[i]) <= EPS) { lost = true; break; }
            if (lost) break;
            if (step == n) break;                           // 第 n+1 個交點就是答案

            double x1 = VX[bi], y1 = VY[bi];
            double x2 = VX[(bi + 1) % v], y2 = VY[(bi + 1) % v];
            double ex = x2 - x1, ey = y2 - y1;
            double L = hypot(ex, ey);
            double nx = -ey / L, ny = ex / L;               // 單位法向量
            double dot = dx * nx + dy * ny;
            dx -= 2 * dot * nx;
            dy -= 2 * dot * ny;
            px = hx; py = hy;
        }

        if (lost) cout << "lost forever...\n";
        else cout << hx << " " << hy << "\n";
    }
    return 0;
}`
  }
};
