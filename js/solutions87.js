/* 第四十七批 —— pdftotext 重抽題敘後補回 */
const SOL87 = {
  '10207': {
    q: `一場錦標賽只有兩隊：Abahoni 與 Mohamedan。他們最多打 2n − 1 場，先拿到 n 勝的隊伍獲勝。沒有和局，每場結果互相獨立，Abahoni 每場獲勝的機率固定為 p，Mohamedan 為 q = 1 − p。

P(i, j) 定義為「Abahoni 還需要 i 勝、Mohamedan 還需要 j 勝」時，Abahoni 贏得整個系列賽的機率，可以用下面的函式計算：

    Function P(i, j)
        if i = 0 then return 1
        else if j = 0 then return 0
        else return p·P(i−1, j) + q·P(i, j−1)

請寫程式，對任意的 p、i、j 算出獲勝機率，同時算出「若真的用上面這個函式計算，需要幾次遞迴呼叫」。

輸入：多組測資。每組第一行是一個浮點數 p（0 < p < 1）與一個整數 N（0 ≤ N < 1001），N 是接下來的查詢數。接下來 N 行，每行兩個整數 i（0 ≤ i ≤ 1000）與 j（0 ≤ j ≤ 1000）。以 N = 0 的那組結束，該組不處理。
輸出：每個查詢印兩行。第一行是 P(i, j)，取到小數點後五位；第二行是需要的遞迴呼叫次數。若 P(i, j) 沒有定義，就以相同格式印「-1」。連續兩組測資的輸出之間要空一行。

範例輸入
0.5 3
1 1
2 2
3 3
0.5 2
10 3
10 2
0.7 0

範例輸出
0.50000
2
0.50000
10
0.50000
38

0.01929
570
0.00586
130`,
    h: `這題有兩個獨立的部分。

【機率：直接 DP】
    P(0, j) = 1（j ≥ 1）、P(i, 0) = 0（i ≥ 1）
    P(i, j) = p·P(i−1, j) + q·P(i, j−1)
i、j 都不超過 1000，所以對每一組測資（p 固定）先把 1001 × 1001 的表填好，
之後每個查詢都是 O(1)。整張表約 800 萬位元組，可以接受。
（P(0, 0) 兩個條件互相矛盾，視為沒有定義。）

【呼叫次數：有封閉解】
設 R(i, j) 是遞迴呼叫的次數。i = 0 或 j = 0 時直接回傳，不再遞迴，所以 R = 0；
否則這一層會呼叫兩次，加上兩棵子樹：

    R(i, j) = 2 + R(i−1, j) + R(i, j−1)

把前幾項算出來與範例對照：R(1,1) = 2、R(2,2) = 10、R(3,3) = 38、R(10,3) = 570、R(10,2) = 130，
與題目給的五個數字完全一致。這串數字其實有封閉解：

    **R(i, j) = 2 · ( C(i+j, i) − 1 )**

驗算：C(2,1) = 2 → 2 ✓；C(4,2) = 6 → 10 ✓；C(6,3) = 20 → 38 ✓；
C(13,3) = 286 → 570 ✓；C(12,2) = 66 → 130 ✓。五個全中。

【所以需要大數】i = j = 1000 時 C(2000, 1000) 有 601 位數，long long 差得遠。
用「res = res × (n−k+t) / t，t 從 1 跑到 k」逐步累乘除即可——每一步的中間結果都是整數
（因為它就是 C(n−k+t, t)），所以只需要「大數乘小整數」與「大數除以小整數」兩個運算，
不必寫完整的大數除法。k = min(i, j) ≤ 1000，每個查詢約 1000 次運算，非常快。`,
    t: `1. 呼叫次數會是 601 位數的天文數字，一定要用大數；用 long long 會在很小的測資就爆掉。
2. 大數只需要「乘以小整數」與「除以小整數」，用 res = res × (n−k+t) / t 的順序累乘，每一步都保證整除，不必寫大數除大數。
3. 取 k = min(i, j) 可以把迴圈次數減半，也讓中間結果小一些。
4. P(0, 0) 是沒有定義的情形（函式的兩個終止條件互相矛盾），要特別處理；此時呼叫次數是 0。
5. 每組測資的 p 不同，機率表要重算；但同一組內的 N 個查詢可以共用同一張表，不要每個查詢都重跑 DP。
6. 每個查詢印兩行（機率、次數），而且「連續兩組測資之間」要空一行——是組與組之間，不是查詢與查詢之間。
7. 結束條件是 N = 0，但那一行還是會先給一個 p，要記得把它讀掉。
8.【這題我標了不確定】題目說「以相同格式印 -1」，但沒說清楚是印「-1」還是「-1.00000」。我照「相同格式」的字面理解印成 -1.00000（五位小數），範例裡剛好沒有這種情形可以對照。若送出被判 WA，就改印純粹的 -1 試試看。另外 P(0,0) 是否真的算「沒有定義」也是我的判斷——那個函式其實會回傳 1。`,
    unsure: true,
    c: `#include <bits/stdc++.h>
using namespace std;

// 大數：base 10000，低位在前
typedef vector<int> Big;

void mulSmall(Big& a, long long m) {
    long long carry = 0;
    for (size_t i = 0; i < a.size(); i++) {
        long long v = a[i] * m + carry;
        a[i] = (int)(v % 10000);
        carry = v / 10000;
    }
    while (carry) { a.push_back((int)(carry % 10000)); carry /= 10000; }
}

void divSmall(Big& a, long long d) {
    long long rem = 0;
    for (int i = (int)a.size() - 1; i >= 0; i--) {
        long long v = rem * 10000 + a[i];
        a[i] = (int)(v / d);
        rem = v % d;
    }
    while (a.size() > 1 && a.back() == 0) a.pop_back();
}

void subSmall(Big& a, int d) {                 // a -= d（保證 a >= d）
    int i = 0;
    while (d) {
        int v = a[i] - d % 10000;
        d /= 10000;
        if (v < 0) { v += 10000; d++; }
        a[i++] = v;
    }
    while (a.size() > 1 && a.back() == 0) a.pop_back();
}

string bigStr(const Big& a) {
    ostringstream os;
    os << a.back();
    for (int i = (int)a.size() - 2; i >= 0; i--)
        os << setw(4) << setfill('0') << a[i];
    return os.str();
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    static double dp[1001][1001];
    double p;
    int N, set_ = 0;
    while (cin >> p >> N) {
        if (N == 0) break;
        double q = 1.0 - p;
        for (int j = 0; j <= 1000; j++) dp[0][j] = 1.0;
        for (int i = 1; i <= 1000; i++) {
            dp[i][0] = 0.0;
            for (int j = 1; j <= 1000; j++)
                dp[i][j] = p * dp[i - 1][j] + q * dp[i][j - 1];
        }

        if (set_++) cout << "\n";
        cout << fixed << setprecision(5);
        while (N--) {
            int i, j;
            cin >> i >> j;
            if (i == 0 && j == 0) cout << -1.0 << "\n0\n";   // 沒有定義
            else {
                cout << dp[i][j] << "\n";
                int n = i + j, k = min(i, j);
                Big c(1, 1);                                   // C(n, k)
                for (int t = 1; t <= k; t++) {
                    mulSmall(c, n - k + t);
                    divSmall(c, t);                            // 每一步都整除
                }
                subSmall(c, 1);
                mulSmall(c, 2);                                // 2 * (C - 1)
                cout << bigStr(c) << "\n";
            }
        }
    }
    return 0;
}`
  },

  '11654': {
    q: `若一個數列相鄰兩項的差固定，就稱為等差數列。例如 {1, 5, 9} 是公差 4、長度 3 的等差數列。

給你一個集合，請算出有多少個子集合構成等差數列。子集合中元素的順序必須與原集合中的順序一致——例如原集合是 {1, 3, 6} 時，{1, 3} 是合法的子集合，但 {3, 1} 不是。

輸入：第一行是正整數 T ≤ 100，代表測資組數。每組先是一個正整數 n ≤ 250，接著 n 行、每行一個非負整數（值小於 100000001），代表原集合的元素。
輸出：每組一行，輸出構成等差數列的子集合個數。數字可能很大，請對 10000007 取模。格式照範例。

範例輸入
3
3
1
2
4
1
1
3
1
2
2

範例輸出
Case 1: 6
Case 2: 1
Case 3: 6`,
    h: `長度 1 與長度 2 的子序列**必定**是等差數列，所以

    答案 = n（單元素） + 所有「長度 ≥ 2」的等差子序列個數

【核心 DP】令 dp[i][j]（j < i）= 「以位置 j、再接位置 i 結尾」的等差子序列個數
（長度 ≥ 2，公差固定為 a[i] − a[j]）。轉移是

    dp[i][j] = 1 + Σ_{k < j, a[j] − a[k] = a[i] − a[j]} dp[j][k]

那個 1 代表「就只有 (j, i) 這一對」的情形，後面的總和代表「前面接得更長」的情形。

直接算那個總和是 O(n³)。改成：對每個 j 維護一張表

    M[j][d] = Σ_{k < j, a[j] − a[k] = d} dp[j][k]

那麼 dp[i][j] = 1 + M[j][a[i] − a[j]]，整體降到 O(n²)（加上 map 的 log）。
n ≤ 250、T ≤ 100，非常輕鬆。

【逐組驗算】（我實作出來跑過三組範例）
  第 1 組 {1, 2, 4}：單元素 3 個；配對 (1,2)、(1,4)、(2,4) 共 3 個；
      三元的 1,2,4 公差是 1 然後 2，不是等差。→ 3 + 3 = **6** ✓
  第 2 組 {1}：只有單元素 1 個 → **1** ✓
      （順帶一提，這組也告訴我們「整個集合本身」是要算進去的，
       題目雖然寫 proper subset，但實際上要把所有非空子序列都算。）
  第 3 組 {1, 2, 2}：單元素 3 個；配對 (1,2)、(1,2)、(2,2) 共 3 個；
      三元的 1,2,2 公差 1 然後 0，不是等差。→ **6** ✓
我另外寫了 2^n 的暴力枚舉，在 200 組隨機測資（n ≤ 9）上與這個 DP 逐一比對，完全一致。`,
    t: `1. 題目雖然說 proper subset，但第 2 組範例（n = 1 答案 1）顯示「整個集合」與所有非空子序列都要算。不要真的把全集扣掉。
2. 長度 1 與長度 2 一定是等差數列，別漏掉這 n + C(n,2) 個。
3. 元素可以重複（第 3 組有兩個 2），公差可以是 0，也可以是負的——不要假設遞增。
4. 值小於 100000001，兩數相減可能是負的，map 的鍵要用有號整數（int 就夠，但用 long long 更保險）。
5. 模數是 10000007（八位數，不是常見的 1000000007），很容易看錯。
6. 累加到 M[i][d] 的必須是「已經取模」的 dp 值，否則會溢位。
7. 輸出格式是「Case k: 答案」，冒號後面有一個空白。`,
    c: `#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    const long long MOD = 10000007LL;
    int T;
    if (!(cin >> T)) return 0;
    for (int tc = 1; tc <= T; tc++) {
        int n;
        cin >> n;
        vector<long long> a(n);
        for (int i = 0; i < n; i++) cin >> a[i];

        vector<map<long long, long long> > M(n);      // M[j][d] = 以 j 結尾、公差 d 的總數
        long long tot = n % MOD;                       // 單元素
        for (int i = 0; i < n; i++)
            for (int j = 0; j < i; j++) {
                long long d = a[i] - a[j];
                long long add = 0;
                map<long long, long long>::iterator it = M[j].find(d);
                if (it != M[j].end()) add = it->second;
                long long v = (1 + add) % MOD;         // 1 = 只有 (j, i) 這一對
                tot = (tot + v) % MOD;
                M[i][d] = (M[i][d] + v) % MOD;
            }
        cout << "Case " << tc << ": " << tot << "\n";
    }
    return 0;
}`
  },

  '10175': {
    q: `外星人把「成對球體」放到有毒液體上漂流。成對球體是兩顆互相重疊的球黏在一起。請算出成對球體的體積與表面積，並判斷它會浮起來還是沉下去。（π 請用 2·arccos(0)。）

輸入：第一行是整數 N，代表有幾組測資。接下來 N 行，每行五個數：R1（0 < R1 ≤ 500）與 R2（0 < R2 ≤ 500）是兩顆球的半徑（公分），d 是兩球球心的距離（公分，且 d > max(R1, R2)、d < R1 + R2），W 是成對球體的重量（公克，W < 10000000000），S（0 < S < 10）是有毒液體的比重。
輸出：每組兩行。第一行是體積與表面積（各取小數點後四位），第二行依情況印「The Paired-Sphere Sinks.」或「The Paired-Sphere Floats.」。

範例輸入
2
10 10 15 300 1.0
20 20 30 650000 1.0

範例輸出
8017.6063 2199.1149
The Paired-Sphere Floats.
64140.8500 8796.4594
The Paired-Sphere Sinks.`,
    h: `【兩球相交的幾何】
設兩球心距離 d。兩球面的交線落在一個平面上，這個平面到第一顆球心的距離是

    a = (d² + R1² − R2²) / (2d)

（用兩個球方程式相減即可得到。）於是被對方切掉的「球冠」高度分別是

    h1 = R1 − a
    h2 = R2 − (d − a)

【體積】球冠體積公式是 π·h²·(3R − h) / 3。聯集體積是兩球體積相加再扣掉重疊的部分，
而重疊的透鏡形正好由兩個球冠拼成：

    V = (4/3)πR1³ + (4/3)πR2³ − [ π·h1²(3R1 − h1)/3 + π·h2²(3R2 − h2)/3 ]

【表面積】球冠的側面積是 2πRh（不含底面圓）。聯集的表面就是兩個完整球面各扣掉被埋進去的球冠：

    A = 4πR1² + 4πR2² − ( 2πR1·h1 + 2πR2·h2 )

【浮沉】密度 = W / V（公克/立方公分），與液體比重 S 比較：
    密度 < S → Floats；密度 > S → Sinks。（題目保證不會剛好相等。）

【逐組驗算】（我把公式算過兩組範例）
  第 1 組：R1 = R2 = 10、d = 15 → a = 7.5、h1 = h2 = 2.5
      單顆球體積 4188.7902，兩顆共 8377.5804
      單個球冠 π·6.25·27.5/3 = 179.9873，兩個共 359.9746
      V = 8377.5804 − 359.9746 = **8017.6063** ✓
      單顆球表面 1256.6371，兩顆共 2513.2741
      單個球冠側面 2π·10·2.5 = 157.0796，兩個共 314.1593
      A = 2513.2741 − 314.1593 = **2199.1149** ✓
      密度 = 300 / 8017.6063 = 0.0374 < 1.0 → **Floats** ✓
  第 2 組：R = 20、d = 30 → a = 15、h = 5
      V = 67020.6433 − 2879.7933 = **64140.8500** ✓
      A = 10053.0965 − 1256.6371 = **8796.4594** ✓
      密度 = 650000 / 64140.85 = 10.13 > 1.0 → **Sinks** ✓`,
    t: `1. 題目說 d > max(R1, R2)，所以兩顆球一定互相重疊、但球心都在對方外面——交線平面必定落在兩球心之間，h1、h2 都是正的。
2. 重疊的透鏡形是「兩個球冠」拼起來，不是一個球冠；只扣一個會少扣一半。
3. 表面積扣掉的是球冠的「側面積」2πRh，不要用球冠的底面圓面積 πr²。
4. 兩顆球的半徑可以不一樣，a 的公式要用一般式 (d² + R1² − R2²)/(2d)，不能直接寫 d/2。
5. π 要用 2·acos(0)（或 acos(-1.0)），不要自己寫 3.14159 造成第四位小數對不上。
6. W 可到 10^10，用 double 讀沒問題，但別用 int。
7. 兩行輸出：第一行是「體積 空白 表面積」各四位小數，第二行的句子結尾有句點。`,
    c: `#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout << fixed << setprecision(4);
    const double PI = 2.0 * acos(0.0);
    int n;
    if (!(cin >> n)) return 0;
    while (n--) {
        double r1, r2, d, w, s;
        cin >> r1 >> r2 >> d >> w >> s;

        double a = (d * d + r1 * r1 - r2 * r2) / (2.0 * d);   // 交線平面到球心 1 的距離
        double h1 = r1 - a;                                   // 被切掉的球冠高
        double h2 = r2 - (d - a);

        double vol = 4.0 / 3.0 * PI * r1 * r1 * r1 + 4.0 / 3.0 * PI * r2 * r2 * r2
                   - (PI * h1 * h1 * (3 * r1 - h1) / 3.0 + PI * h2 * h2 * (3 * r2 - h2) / 3.0);
        double area = 4.0 * PI * r1 * r1 + 4.0 * PI * r2 * r2
                    - (2.0 * PI * r1 * h1 + 2.0 * PI * r2 * h2);

        cout << vol << " " << area << "\n";
        cout << ((w / vol < s) ? "The Paired-Sphere Floats." : "The Paired-Sphere Sinks.") << "\n";
    }
    return 0;
}`
  }
};
