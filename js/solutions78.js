/* 第三十八批 —— pdftotext 重抽題敘後補回 */
const SOL78 = {
  '10011': {
    q: `Cartesia 星球的表面是完美的平面。星球中心 (0, 0) 有一個「沒有大小」的輻射源，輻射沿著直線往外擴散。Cartesia 人小到可以看成一個點，只要一點點輻射就會受害。

幸好星球上有樹可以擋住輻射。樹是完美的圓形，有給定的半徑。若一個 Cartesia 人與輻射源之間的線段「穿過某棵樹」，他就是安全的。

Cartesia 人喜歡把房子蓋在樹蔭下。給你「一棵樹」與「一間房子的位置」，請求出：從房子出發沿直線走，在「保證受到保護」的前提下最遠能走多遠。注意 Cartesia 人不能穿過樹。

輸入：第一行是一個正整數 n，代表接下來的資料行數。接下來 n 行、每行五個小數，以單一空白分隔：前兩個 (x, y) 是樹的位置，第三個 r（r > 0）是樹的半徑，最後兩個 (u, v) 是房子的位置。輻射源在 (0, 0)。房子與輻射源都不在樹的內部。
輸出：每行輸出一個小數，取到小數點後三位。若值小於 1.000 才要有前導的 0（0.123 合法、.123 不合法）。若完全無法安全離開房子，輸出 0.000。

範例輸入
3
5 5 1 12 12
20 0 3 7 -7
-9.6 4.3 2.1 -19.2 7.9

範例輸出
2.400
0.000
3.517`,
    h: `【安全區域長什麼樣】
從原點看這棵圓形的樹，會拉出兩條切線；樹的「影子」就是這兩條切線之間、而且在樹後方的那個無限扇形區域。

這裡有個很容易誤會的地方：影子是**無限延伸**的，所以「沿著遠離原點的方向直走」可以走到無限遠。既然答案是有限值，題目問的其實是

    **從房子出發，不管往哪個方向走，都保證安全的最遠距離**

也就是「房子到安全區域邊界的最短距離」（安全區域的內切半徑）。

【邊界有三個部分】
設 C 是樹心、d = |C|、R = |房子|、r 是半徑。半角

    α = asin(r / d)

θ = 房子方向與樹心方向的夾角。那麼

    到切線一的距離 = R · sin(α − θ)
    到切線二的距離 = R · sin(α + θ)
    到樹本身的距離 = |房子 − C| − r      （不能穿過樹）

答案就是這三者取最小值。

【先判斷房子在不在影子裡】
    θ ≥ α                → 不在扇形內 → 0.000
    R < 樹的「近側交點」距離 → 房子在樹的前面，沒被擋到 → 0.000
近側交點距離 = d·cos θ − √(r² − d²sin²θ)。

【逐組驗算】（我實作出來跑過三組，全中）
  第 1 組：樹 (5,5) r=1、房子 (12,12)。d = √50 ≈ 7.0711、α = asin(1/7.0711) = 8.130°。
      房子跟樹心同方向（θ = 0），R = √288 ≈ 16.9706。
      到切線 = R·sin α = 16.9706 × 0.141421 = **2.400** ✓
      （到樹的距離是 √98 − 1 ≈ 8.899，比較大所以不是答案。）
  第 2 組：樹 (20,0) r=3，α = asin(3/20) = 8.627°；房子 (7,−7) 在 −45° 方向，θ = 45° > α
      → 根本不在影子裡 → **0.000** ✓
  第 3 組：α = 11.518°、θ = 1.758°、R ≈ 20.762
      → 兩條切線分別是 20.762·sin(9.760°) ≈ 3.517 與 20.762·sin(13.276°) ≈ 4.768，
        到樹是 10.253 − 2.1 ≈ 8.153 → 最小 **3.517** ✓`,
    t: `1. 最容易想歪的地方：影子是無限扇形，「沿著背離原點的方向」可以走無限遠。題目要的是「不論往哪個方向都安全」的距離，也就是到邊界的最短距離。
2. 邊界有三段，別漏掉「樹本身」——人不能穿過樹，房子貼著樹時答案是 |房子 − 樹心| − r。
3. 求夾角用 atan2(cross, dot) 比 acos(dot/…) 穩定得多。
4. 要判斷房子是不是真的在影子裡：除了 θ < α，還要確認房子在樹的「後面」（距離大於近側交點），否則房子在樹前面其實沒被擋到。
5. 輸出固定三位小數，值小於 1 時要印前導 0——用 cout << fixed << setprecision(3) 就自然符合。
6. 輸入是小數不是整數（第三組有 −9.6、2.1），一定要用 double 讀。
7. 第一行的 n 是「資料行數」，不是測資組數的其他意思，照著讀 n 行即可。`,
    c: `#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout << fixed << setprecision(3);
    int n;
    if (!(cin >> n)) return 0;
    while (n--) {
        double x, y, r, u, v;
        cin >> x >> y >> r >> u >> v;
        double d = sqrt(x * x + y * y);          // 原點到樹心
        double R = sqrt(u * u + v * v);          // 原點到房子
        if (d <= r || R <= r) { cout << 0.0 << "\n"; continue; }

        double a = asin(min(1.0, r / d));        // 影子的半角
        double th = fabs(atan2(x * v - y * u, x * u + y * v));   // 房子偏離樹心方向的角度
        if (th >= a) { cout << 0.0 << "\n"; continue; }

        double s = d * sin(th);
        double near_ = d * cos(th) - sqrt(max(0.0, r * r - s * s));  // 樹的近側交點
        if (R < near_) { cout << 0.0 << "\n"; continue; }           // 房子在樹前面

        double d1 = R * sin(a - th);             // 到切線一
        double d2 = R * sin(a + th);             // 到切線二
        double d3 = sqrt((u - x) * (u - x) + (v - y) * (v - y)) - r;  // 到樹
        cout << min(d1, min(d2, d3)) << "\n";
    }
    return 0;
}`
  },

  '10123': {
    q: `一根均勻、筆直的木板放在兩個等高的支點上。木板中點是質心，位置記為 0；支點固定在 −1.5 與 +1.5。板上放了若干包裹，每個包裹有位置與重量。

物體對某支點的力矩 = 重量 × 到該支點的距離；在支點左邊是逆時針、右邊是順時針。把板上所有物體（包含木板本身的重量，作用在位置 0）的力矩加總，就是繞該支點的淨力矩。

若「繞左支點的淨力矩是逆時針」或「繞右支點的淨力矩是順時針」，木板就會翻倒。請每次拿走一個包裹，找出一種順序使得整個過程中木板都不會翻倒。

輸入：多組測資。每組第一行是三個整數：木板長度（公尺，至少 3）、木板重量（公斤）、包裹數 n（n ≤ 20）。接下來 n 行、每行兩個整數：包裹在板上的位置（從中心量起，負數代表左邊）與重量。以一行三個 0 結束。
輸出：每組先印「Case k:」，接著 n 行、每行兩個整數（位置與重量），代表一個不會翻倒的移除順序。若無解，只印一行「Impossible」。若一開始就不平衡，就是無解。

範例輸入
20 3 6
-8 4
-4 10
-3 10
2 4
5 7
8 8
20 3 15
1 10
8 5
-6 8
5 9
-8 4
8 10
-3 10
-4 5
2 9
-2 2
3 3
-3 2
5 1
-6 1
2 5
30 10 2
-8 100
9 91
0 0 0

範例輸出
Case 1:
-4 10
8 8
-8 4
5 7
-3 10
2 4
Case 2:
（依序輸出 15 個包裹，剛好就是輸入的順序）
Case 3:
Impossible`,
    h: `【先把「不翻倒」寫成兩個不等式】
繞左支點（位置 −1.5）：物體 (p, w) 貢獻 w·(p + 1.5)，往右是順時針取正。
「不可以逆時針」就是

    L = Σ w·(p + 1.5) ≥ 0

繞右支點（位置 +1.5）：貢獻 w·(p − 1.5)，「不可以順時針」就是

    R = Σ w·(p − 1.5) ≤ 0

木板本身重 W、作用在 0，所以固定貢獻 L += W·1.5、R += W·(−1.5)。
（實作時把所有東西乘 2，用 w·(2p+3) 與 w·(2p−3)，就完全是整數運算，不會有浮點誤差。）

【搜尋】
n ≤ 20，而且 L、R 只跟「還剩哪些包裹」有關（跟拿走的順序無關），所以狀態就是一個 20 位元的
bitmask。用 DFS + 記憶化「這個 mask 走不下去」即可：

    最多 2^20 ≈ 100 萬個狀態，每個狀態試 20 種移除 → 2000 萬次，很快。

沒有記憶化的純回溯在最壞情況會退化成 20!，一定要記得記。

【驗算】（我把它跑過三組範例）
  第 1 組：找到 −8 4 / 8 8 / −4 10 / 5 7 / −3 10 / 2 4，每一步都滿足兩個不等式。
      （題目範例給的是 −4 / 8 / −8 / 5 / −3 / 2，兩個都合法——見下面的陷阱說明。）
  第 2 組：輸出跟輸入順序一模一樣，與題目範例完全相同 ✓
  第 3 組：初始是平衡的（L = 320.5、R = −282.5），但兩個包裹都拿不掉——
      拿走 (−8, 100) 會讓 R 變成 +667.5（往右翻）、拿走 (9, 91) 會讓 L 變成 −635（往左翻）
      → Impossible ✓`,
    t: `1. 兩個條件方向不同：左支點是「≥ 0」、右支點是「≤ 0」。寫反了會兩組都錯。
2. 別忘了木板自己的重量，它固定作用在位置 0，對左支點是 +1.5W、對右支點是 −1.5W。
3. 支點在 ±1.5 會產生小數，把所有力矩乘 2 改用 w·(2p±3)，全程整數、零浮點誤差。
4. 一定要記憶化失敗的 mask，否則最壞會變成 20! 的回溯。
5. 「一開始就不平衡」也算 Impossible，記得先檢查初始狀態。
6. 木板長度這個輸入其實用不到（只用來保證包裹位置在板上），讀進來丟掉即可。
7. 結束條件是「三個 0」的那一行。
8.【這題我標了不確定】合法的移除順序常常不只一種，我的程式在第一組找到的順序與題目範例不同（兩者都合法）。題敘只說「輸出一個可以移除的順序」，看起來是特判；但若送出被判 WA，就代表評測要求特定順序，這時可以改成「照輸入順序優先嘗試」等固定策略再試（第二組範例的答案剛好就是輸入順序）。`,
    unsure: true,
    c: `#include <bits/stdc++.h>
using namespace std;

int n_;
long long P_[20], W_[20];
vector<char> bad_;
vector<int> ord_;

bool dfs(int mask, long long L, long long R) {
    if (mask == (1 << n_) - 1) return true;
    if (bad_[mask]) return false;
    for (int i = 0; i < n_; i++) {
        if (mask & (1 << i)) continue;
        long long nL = L - W_[i] * (2 * P_[i] + 3);
        long long nR = R - W_[i] * (2 * P_[i] - 3);
        if (nL < 0 || nR > 0) continue;              // 會翻倒
        ord_.push_back(i);
        if (dfs(mask | (1 << i), nL, nR)) return true;
        ord_.pop_back();
    }
    bad_[mask] = 1;                                  // 這個狀態走不下去
    return false;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    long long len, bw;
    int cs = 0;
    while (cin >> len >> bw >> n_) {
        if (len == 0 && bw == 0 && n_ == 0) break;
        long long L = bw * 3, R = -bw * 3;           // 全部力矩乘 2
        for (int i = 0; i < n_; i++) {
            cin >> P_[i] >> W_[i];
            L += W_[i] * (2 * P_[i] + 3);
            R += W_[i] * (2 * P_[i] - 3);
        }
        cout << "Case " << ++cs << ":\n";
        if (L < 0 || R > 0) { cout << "Impossible\n"; continue; }
        bad_.assign(1 << n_, 0);
        ord_.clear();
        if (!dfs(0, L, R)) { cout << "Impossible\n"; continue; }
        for (size_t k = 0; k < ord_.size(); k++)
            cout << P_[ord_[k]] << " " << W_[ord_[k]] << "\n";
    }
    return 0;
}`
  }
};
